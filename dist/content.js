    }
  });

  // node_modules/budoux/module/data/models/th.js
  var init_th = __esm({
    "node_modules/budoux/module/data/models/th.js"() {
    }
  });

  // node_modules/budoux/module/data/models/zh-hans.js
  var init_zh_hans = __esm({
    "node_modules/budoux/module/data/models/zh-hans.js"() {
    }
  });

  // node_modules/budoux/module/data/models/zh-hant.js
  var init_zh_hant = __esm({
    "node_modules/budoux/module/data/models/zh-hant.js"() {
    }
  });

  // node_modules/budoux/module/dom-browser.js
  var parseFromString;
  var init_dom_browser = __esm({
    "node_modules/budoux/module/dom-browser.js"() {
      parseFromString = (html) => {
        return new window.DOMParser().parseFromString(html, "text/html");
      };
    }
  });

  // node_modules/budoux/module/parser.js
  var Parser;
  var init_parser = __esm({
    "node_modules/budoux/module/parser.js"() {
      Parser = class {
        /**
         * Constructs a BudouX parser.
         * @param model A model data.
         */
        constructor(model5) {
          this.model = new Map(Object.entries(model5).map(([k, v]) => [
            k,
            new Map(Object.entries(v))
          ]));
          this.baseScore = -0.5 * [...this.model.values()].flatMap((group) => [...group.values()]).reduce((prev, curr) => prev + curr, 0);
        }
        /**
         * Parses the input sentence and returns a list of semantic chunks.
         *
         * @param sentence An input sentence.
         * @return The retrieved chunks.
         */
        parse(sentence) {
          if (sentence === "")
            return [];
          const boundaries = this.parseBoundaries(sentence);
          const result = [];
          let start = 0;
          for (const boundary of boundaries) {
            result.push(sentence.slice(start, boundary));
            start = boundary;
          }
          result.push(sentence.slice(start));
          return result;
        }
        /**
         * Parses the input sentence and returns a list of boundaries.
         *
         * @param sentence An input sentence.
         * @return The list of boundaries.
         */
        parseBoundaries(sentence) {
          var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o;
          const result = [];
          for (let i = 1; i < sentence.length; i++) {
            let score = this.baseScore;
            score += ((_a = this.model.get("UW1")) === null || _a === void 0 ? void 0 : _a.get(sentence.substring(i - 3, i - 2))) || 0;
            score += ((_b = this.model.get("UW2")) === null || _b === void 0 ? void 0 : _b.get(sentence.substring(i - 2, i - 1))) || 0;
            score += ((_c = this.model.get("UW3")) === null || _c === void 0 ? void 0 : _c.get(sentence.substring(i - 1, i))) || 0;
            score += ((_d = this.model.get("UW4")) === null || _d === void 0 ? void 0 : _d.get(sentence.substring(i, i + 1))) || 0;
            score += ((_e = this.model.get("UW5")) === null || _e === void 0 ? void 0 : _e.get(sentence.substring(i + 1, i + 2))) || 0;
            score += ((_f = this.model.get("UW6")) === null || _f === void 0 ? void 0 : _f.get(sentence.substring(i + 2, i + 3))) || 0;
            score += ((_g = this.model.get("BW1")) === null || _g === void 0 ? void 0 : _g.get(sentence.substring(i - 2, i))) || 0;
            score += ((_h = this.model.get("BW2")) === null || _h === void 0 ? void 0 : _h.get(sentence.substring(i - 1, i + 1))) || 0;
            score += ((_j = this.model.get("BW3")) === null || _j === void 0 ? void 0 : _j.get(sentence.substring(i, i + 2))) || 0;
            score += ((_k = this.model.get("TW1")) === null || _k === void 0 ? void 0 : _k.get(sentence.substring(i - 3, i))) || 0;
            score += ((_l = this.model.get("TW2")) === null || _l === void 0 ? void 0 : _l.get(sentence.substring(i - 2, i + 1))) || 0;
            score += ((_m = this.model.get("TW3")) === null || _m === void 0 ? void 0 : _m.get(sentence.substring(i - 1, i + 2))) || 0;
            score += ((_o = this.model.get("TW4")) === null || _o === void 0 ? void 0 : _o.get(sentence.substring(i, i + 3))) || 0;
            if (score > 0)
              result.push(i);
          }
          return result;
        }
      };
    }
  });

  // node_modules/budoux/module/html_processor.js
  function actionForDisplay(display) {
    if (display === "inline")
      return DomAction.Inline;
    if (display === "block")
      return DomAction.Block;
    if (display.startsWith("ruby")) {
      if (display === "ruby-text")
        return DomAction.Skip;
      return DomAction.Inline;
    }
    return DomAction.Block;
  }
  function actionForElement(element) {
    const nodeName = element.nodeName;
    const action = domActions[nodeName];
    if (action !== void 0)
      return action;
    if (typeof globalThis.getComputedStyle === "function") {
      const style = globalThis.getComputedStyle(element);
      switch (style.whiteSpace) {
        case "nowrap":
        case "pre":
          return DomAction.NoBreak;
      }
      const display = style.display;
      if (display)
        return actionForDisplay(display);
    }
    return defaultBlockElements.has(nodeName) ? DomAction.Block : DomAction.Inline;
  }
  var assert, ZWSP_CODEPOINT, ZWSP, NodeType, DomAction, domActions, defaultBlockElements, applyWrapStyle, NodeOrText, Paragraph, HTMLProcessor, HTMLProcessingParser;
  var init_html_processor = __esm({
    "node_modules/budoux/module/html_processor.js"() {
      init_dom_browser();
      init_parser();
      assert = console.assert;
      ZWSP_CODEPOINT = 8203;
      ZWSP = String.fromCharCode(ZWSP_CODEPOINT);
      NodeType = {
        ELEMENT_NODE: 1,
        TEXT_NODE: 3
      };
      DomAction = {
        Inline: 0,
        // An inline content, becomes a part of a paragraph.
        Block: 1,
        // A nested paragraph.
        Skip: 2,
        // Skip the content. The content before and after are connected.
        Break: 3,
        // A forced break. The content before and after become paragraphs.
        NoBreak: 4,
        // The content provides context, but it's not breakable.
        BreakOpportunity: 5
        // Force a break opportunity.
      };
      domActions = {
        // Hidden elements
        // https://html.spec.whatwg.org/multipage/rendering.html#hidden-elements
        AREA: DomAction.Skip,
        BASE: DomAction.Skip,
        BASEFONT: DomAction.Skip,
        DATALIST: DomAction.Skip,
        HEAD: DomAction.Skip,
        LINK: DomAction.Skip,
        META: DomAction.Skip,
        NOEMBED: DomAction.Skip,
        NOFRAMES: DomAction.Skip,
        PARAM: DomAction.Skip,
        RP: DomAction.Skip,
        SCRIPT: DomAction.Skip,
        STYLE: DomAction.Skip,
        TEMPLATE: DomAction.Skip,
        TITLE: DomAction.Skip,
        NOSCRIPT: DomAction.Skip,
        // Flow content
        // https://html.spec.whatwg.org/multipage/rendering.html#flow-content-3
        HR: DomAction.Break,
        // Disable if `white-space: pre`.
        LISTING: DomAction.Skip,
        PLAINTEXT: DomAction.Skip,
        PRE: DomAction.Skip,
        XMP: DomAction.Skip,
        // Phrasing content
        // https://html.spec.whatwg.org/multipage/rendering.html#phrasing-content-3
        BR: DomAction.Break,
        RT: DomAction.Skip,
        WBR: DomAction.BreakOpportunity,
        // Form controls
        // https://html.spec.whatwg.org/multipage/rendering.html#form-controls
        INPUT: DomAction.Skip,
        SELECT: DomAction.Skip,
        BUTTON: DomAction.Skip,
        TEXTAREA: DomAction.Skip,
        // Other elements where the phrase-based line breaking should be disabled.
        // https://github.com/google/budoux/blob/main/budoux/skip_nodes.json
        ABBR: DomAction.Skip,
        CODE: DomAction.Skip,
        IFRAME: DomAction.Skip,
        TIME: DomAction.Skip,
        VAR: DomAction.Skip,
        // Deprecated, but supported in all browsers.
        // https://developer.mozilla.org/en-US/docs/Web/HTML/Element/nobr
        NOBR: DomAction.NoBreak
      };
      defaultBlockElements = /* @__PURE__ */ new Set([
        // 15.3.2 The page
        "HTML",
        "BODY",
        // 15.3.3 Flow content
        "ADDRESS",
        "BLOCKQUOTE",
        "CENTER",
        "DIALOG",
        "DIV",
        "FIGURE",
        "FIGCAPTION",
        "FOOTER",
        "FORM",
        "HEADER",
        "LEGEND",
        "LISTING",
        "MAIN",
        "P",
        // 15.3.6 Sections and headings
        "ARTICLE",
        "ASIDE",
        "H1",
        "H2",
        "H3",
        "H4",
        "H5",
        "H6",
        "HGROUP",
        "NAV",
        "SECTION",
        // 15.3.7 Lists
        "DIR",
        "DD",
        "DL",
        "DT",
        "MENU",
        "OL",
        "UL",
        "LI",
        // 15.3.8 Tables
        "TABLE",
        "CAPTION",
        "COL",
        "TR",
        "TD",
        "TH",
        // 15.3.12 The fieldset and legend elements
        "FIELDSET",
        // 15.5.4 The details and summary elements
        "DETAILS",
        "SUMMARY",
        // 15.5.12 The marquee element
        "MARQUEE"
      ]);
      applyWrapStyle = (element) => {
        element.style.wordBreak = "keep-all";
        element.style.overflowWrap = "anywhere";
      };
      NodeOrText = class _NodeOrText {
        constructor(nodeOrText) {
          this.chunks = [];
          this.hasBreakOpportunityAfter = false;
          this.nodeOrText = nodeOrText;
        }
        static isString(value) {
          return typeof value === "string";
        }
        get canSplit() {
          return !_NodeOrText.isString(this.nodeOrText);
        }
        get text() {
          return _NodeOrText.isString(this.nodeOrText) ? this.nodeOrText : this.nodeOrText.nodeValue;
        }
        get length() {
          var _a, _b;
          return (_b = (_a = this.text) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0;
        }
        /**
         * Split the {@link Text} in the same way as the {@link chunks}.
         * Joining all {@link chunks} must be equal to {@link text}.
         */
        split(separator) {
          const chunks = this.chunks;
          assert(chunks.length === 0 || chunks.join("") === this.text);
          if (chunks.length <= 1)
            return;
          if (_NodeOrText.isString(this.nodeOrText))
            return;
          const node = this.nodeOrText;
          if (typeof separator === "string") {
            node.nodeValue = chunks.join(separator);
            return;
          }
          const document2 = node.ownerDocument;
          let nodes = [];
          for (const chunk of chunks) {
            if (chunk)
              nodes.push(document2.createTextNode(chunk));
            nodes.push(null);
          }
          nodes.pop();
          nodes = nodes.map((n) => n ? n : separator.cloneNode(true));
          node.replaceWith(...nodes);
        }
      };
      Paragraph = class {
        constructor(element) {
          this.nodes = [];
          this.element = element;
        }
        isEmpty() {
          return this.nodes.length === 0;
        }
        get text() {
          return this.nodes.map((node) => node.text).join("");
        }
        get lastNode() {
          return this.nodes.length ? this.nodes[this.nodes.length - 1] : void 0;
        }
        setHasBreakOpportunityAfter() {
          const lastNode = this.lastNode;
          if (lastNode)
            lastNode.hasBreakOpportunityAfter = true;
        }
        /**
         * @return Indices of forced break opportunities in the source.
         * They can be created by `<wbr>` tag or `&ZeroWidthSpace;`.
         */
        getForcedOpportunities() {
          const opportunities = [];
          let len = 0;
          for (const node of this.nodes) {
            if (node.canSplit) {
              const text = node.text;
              if (text) {
                for (let i = 0; i < text.length; ++i) {
                  if (text.charCodeAt(i) === ZWSP_CODEPOINT) {
                    opportunities.push(len + i + 1);
                  }
                }
              }
            }
            len += node.length;
            if (node.hasBreakOpportunityAfter) {
              opportunities.push(len);
            }
          }
          return opportunities;
        }
        /**
         * @return Filtered {@param boundaries} by excluding
         * {@link getForcedOpportunities} if it's not empty.
         * Otherwise {@param boundaries}.
         */
        excludeForcedOpportunities(boundaries) {
          const forcedOpportunities = this.getForcedOpportunities();
          if (!forcedOpportunities.length)
            return boundaries;
          const set = new Set(forcedOpportunities);
          return boundaries.filter((i) => !set.has(i));
        }
      };
      HTMLProcessor = class {
        /**
         * @param parser A BudouX {@link Parser} to compute semantic line breaks.
         */
        constructor(parser, options) {
          this.separator = ZWSP;
          this.parser_ = parser;
          if (options !== void 0) {
            if (options.className !== void 0)
              this.className = options.className;
            if (options.separator !== void 0)
              this.separator = options.separator;
          }
        }
        /**
         * Checks if the given element has a text node in its children.
         *
         * @param ele An element to be checked.
         * @return Whether the element has a child text node.
         */
        static hasChildTextNode(ele) {
          for (const child of ele.childNodes) {
            if (child.nodeType === NodeType.TEXT_NODE)
              return true;
          }
          return false;
        }
        /**
         * Applies markups for semantic line breaks to the given HTML element.
         *
         * It breaks descendant nodes into paragraphs,
         * and applies the BudouX to each paragraph.
         * @param element The input element.
         */
        applyToElement(element) {
          for (const block of this.getBlocks(element)) {
            assert(!block.isEmpty());
            this.applyToParagraph(block);
          }
        }
        /**
         * Find paragraphs from a given HTML element.
         * @param element The root element to find paragraphs.
         * @param parent The parent {@link Paragraph} if any.
         * @return A list of {@link Paragraph}s.
         */
        *getBlocks(element, parent) {
          assert(element.nodeType === NodeType.ELEMENT_NODE);
          if (this.className && element.classList.contains(this.className))
            return;
          const action = actionForElement(element);
          if (action === DomAction.Skip)
            return;
          if (action === DomAction.Break) {
            if (parent && !parent.isEmpty()) {
              parent.setHasBreakOpportunityAfter();
              yield parent;
              parent.nodes = [];
            }
            assert(!element.firstChild);
            return;
          }
          if (action === DomAction.BreakOpportunity) {
            if (parent)
              parent.setHasBreakOpportunityAfter();
            return;
          }
          assert(action === DomAction.Block || action === DomAction.Inline || action === DomAction.NoBreak);
          const isNewBlock = !parent || action === DomAction.Block;
          const block = isNewBlock ? new Paragraph(element) : parent;
          for (const child of element.childNodes) {
            switch (child.nodeType) {
              case NodeType.ELEMENT_NODE:
                for (const childBlock of this.getBlocks(child, block))
                  yield childBlock;
                break;
              case NodeType.TEXT_NODE:
                if (action === DomAction.NoBreak) {
                  const text = child.nodeValue;
                  if (text) {
                    block.nodes.push(new NodeOrText(text));
                  }
                  break;
                }
                block.nodes.push(new NodeOrText(child));
                break;
            }
          }
          if (isNewBlock && !block.isEmpty())
            yield block;
        }
        /**
         * Apply the BudouX to the given {@link Paragraph}.
         * @param paragraph The {@link Paragraph} to apply.
         */
        applyToParagraph(paragraph) {
          assert(paragraph.nodes.length > 0);
          if (!paragraph.nodes.some((node) => node.canSplit))
            return;
          const text = paragraph.text;
          if (/^\s*$/.test(text))
            return;
          const boundaries = this.parser_.parseBoundaries(text);
          if (boundaries.length <= 0)
            return;
          assert(boundaries[0] > 0);
          assert(boundaries.every((x, i) => i === 0 || x > boundaries[i - 1]));
          assert(boundaries[boundaries.length - 1] < text.length);
          const adjustedBoundaries = paragraph.excludeForcedOpportunities(boundaries);
          adjustedBoundaries.push(text.length + 1);
          this.splitNodes(paragraph.nodes, adjustedBoundaries);
          this.applyBlockStyle(paragraph.element);
        }
        /**
         * Split {@link NodeOrText} at the specified boundaries.
         * @param nodes A list of {@link NodeOrText}.
         * @param boundaries A list of indices of the text to split at.
         */
        splitNodes(nodes, boundaries) {
          var _a;
          assert(boundaries.length > 0);
          assert(boundaries.every((x, i) => i === 0 || x > boundaries[i - 1]));
          const textLen = nodes.reduce((sum, node) => sum + node.length, 0);
          assert(boundaries[boundaries.length - 1] > textLen);
          let boundary_index = 0;
          let boundary = boundaries[0];
          assert(boundary > 0);
          let nodeStart = 0;
          let lastNode = null;
          for (const node of nodes) {
            assert(boundary >= nodeStart);
            assert(node.chunks.length === 0);
            const nodeText = node.text;
            if (!nodeText)
              continue;
            const nodeLength = nodeText.length;
            const nodeEnd = nodeStart + nodeLength;
            assert(!lastNode || lastNode.canSplit);
            if (!node.canSplit) {
              if (lastNode && boundary === nodeStart) {
                if (lastNode.chunks.length === 0)
                  lastNode.chunks.push((_a = lastNode.text) !== null && _a !== void 0 ? _a : "");
                lastNode.chunks.push("");
              }
              while (boundary < nodeEnd) {
                boundary = boundaries[++boundary_index];
              }
              lastNode = null;
              nodeStart = nodeEnd;
              continue;
            }
            lastNode = node;
            if (boundary >= nodeEnd) {
              nodeStart = nodeEnd;
              continue;
            }
            const chunks = node.chunks;
            let chunkStartInNode = 0;
            while (boundary < nodeEnd) {
              const boundaryInNode = boundary - nodeStart;
              assert(boundaryInNode >= chunkStartInNode);
              chunks.push(nodeText.slice(chunkStartInNode, boundaryInNode));
              chunkStartInNode = boundaryInNode;
              boundary = boundaries[++boundary_index];
            }
            assert(chunkStartInNode < nodeLength);
            chunks.push(nodeText.slice(chunkStartInNode));
            nodeStart = nodeEnd;
          }
          assert(nodeStart === textLen);
          assert(boundary_index < boundaries.length);
          assert(boundaries[boundary_index] >= textLen);
          for (const node of nodes) {
            node.split(this.separator);
          }
        }
        /**
         * Applies the block style to the given element.
         * @param element The element to apply the block style.
         */
        applyBlockStyle(element) {
          if (this.className) {
            element.classList.add(this.className);
            return;
          }
          applyWrapStyle(element);
        }
      };
      HTMLProcessingParser = class extends Parser {
        constructor(model5, htmlProcessorOptions = {
          separator: ZWSP
        }) {
          super(model5);
          this.htmlProcessor = new HTMLProcessor(this, htmlProcessorOptions);
        }
        /**
         * Applies markups for semantic line breaks to the given HTML element.
         * @param parentElement The input element.
         */
        applyToElement(parentElement) {
          this.htmlProcessor.applyToElement(parentElement);
        }
        /**
         * Translates the given HTML string to another HTML string with markups
         * for semantic line breaks.
         * @param html An input html string.
         * @return The translated HTML string.
         */
        translateHTMLString(html) {
          if (html === "")
            return html;
          const doc = parseFromString(html);
          if (HTMLProcessor.hasChildTextNode(doc.body)) {
            const wrapper = doc.createElement("span");
            wrapper.append(...doc.body.childNodes);
            doc.body.append(wrapper);
          }
          this.applyToElement(doc.body.childNodes[0]);
          return doc.body.innerHTML;
        }
      };
    }
  });

  // node_modules/budoux/module/index.js
  var loadDefaultJapaneseParser;
  var init_module = __esm({
    "node_modules/budoux/module/index.js"() {
      init_ja();
      init_th();
      init_zh_hans();
      init_zh_hant();
      init_html_processor();
      init_html_processor();
      init_parser();
      loadDefaultJapaneseParser = () => {
        return new HTMLProcessingParser(model);
      };
    }
  });

  // src/content.ts
  var require_content = __commonJS({
    "src/content.ts"() {
      init_module();
      var parser = loadDefaultJapaneseParser();
      var ATTR_HASH = "data-yt-budoux-hash";
      var ATTR_DONE = "data-yt-budoux-done";
      var STORAGE_KEY = "ytBudouxCaptionLineBreakEnabled";
      var TOGGLE_HOST_ID = "yt-budoux-toggle-host";
      var BUDOUX_TOGGLE_STYLE_ID = "yt-budoux-caption-toggle-styles";
      var CLS_HOST = "ytp-budoux-jwbreak-host";
      var CLS_SWITCH = "ytp-budoux-jwbreak-switch";
      var CLS_KNOB = "ytp-budoux-jwbreak-knob";
      var CLS_KNOB_ICON = "ytp-budoux-jwbreak-knob-icon";
      var KNOB_ON_FACE = "#0f0f0f";
      var CLS_TOOLTIP = "ytp-budoux-jwbreak-tooltip";
      var BODY_TOOLTIP_ID = "yt-budoux-body-tooltip";
      var UI_TOGGLE_LABEL = "\u5B57\u5E55\u306E\u6539\u884C";
      var CAPTION_WINDOW_SELECTORS = [
        "#movie_player .ytp-caption-window-container .ytp-caption-window",
        ".ytp-caption-window-container .ytp-caption-window",
        /** 現行プレーヤーは `caption-window ytp-caption-window-bottom` など（`ytp-caption-window` クラス無し） */
        "#movie_player .ytp-caption-window-container .caption-window",
        ".ytp-caption-window-container .caption-window"
      ];
      var MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE = 54;
      var captionBackup = null;
      var tooltipAnchorHost = null;
      var cachedToggleShortcut = "";
      function fetchToggleShortcut(done) {
        try {
          chrome.runtime.sendMessage({ type: "BUDOUX_GET_TOGGLE_SHORTCUT" }, (res) => {
            if (chrome.runtime.lastError) cachedToggleShortcut = "";
            else cachedToggleShortcut = (res && res.shortcut) || "";
            done();
          });
        } catch {
          cachedToggleShortcut = "";
          done();
        }
      }
      function loadBudouxEnabled() {
        try {
          const v = localStorage.getItem(STORAGE_KEY);
          if (v === null) return true;
          return v === "1" || v === "true";
        } catch {
          return true;
        }
      }
      var budouxCaptionEnabled = loadBudouxEnabled();
      function saveBudouxEnabled(on) {
        budouxCaptionEnabled = on;
        try {
          localStorage.setItem(STORAGE_KEY, on ? "1" : "0");
        } catch {
        }
      }
      function queryInMoviePlayer(selector) {
        const mp = document.querySelector("#movie_player");
        if (!mp) return null;
        const queue = [mp];
        if (mp.shadowRoot) queue.push(mp.shadowRoot);
        const seen = /* @__PURE__ */ new Set();
        while (queue.length) {
          const n = queue.shift();
          if (seen.has(n)) continue;
          seen.add(n);
          const found = n.querySelector(selector);
          if (found instanceof HTMLElement) return found;
          n.querySelectorAll("*").forEach((el) => {
            if (el instanceof Element && el.shadowRoot && !seen.has(el.shadowRoot)) {
              queue.push(el.shadowRoot);
            }
          });
        }
        return null;
      }
      function findPlayerVideo() {
        const roots = [
          document.querySelector("#movie_player"),
          document.querySelector("ytd-player"),
          document.querySelector("ytd-watch-flexy"),
          document.querySelector("ytd-shorts")
        ];
        for (const root of roots) {
          if (!root) continue;
          const queue = [root];
          if (root instanceof Element && root.shadowRoot) queue.push(root.shadowRoot);
          const seen = /* @__PURE__ */ new Set();
          while (queue.length) {
            const n = queue.shift();
            if (seen.has(n)) continue;
            seen.add(n);
            const v = n.querySelector?.("video");
            if (v instanceof HTMLVideoElement) return v;
            n.querySelectorAll?.("*").forEach((el) => {
              if (el instanceof Element && el.shadowRoot && !seen.has(el.shadowRoot)) {
                queue.push(el.shadowRoot);
              }
            });
          }
        }
        return null;
      }
      var videosWithCaptionListeners = /* @__PURE__ */ new WeakSet();
      function staggeredScheduleProcess() {
        scheduleProcess();
        window.setTimeout(scheduleProcess, 120);
        window.setTimeout(scheduleProcess, 350);
      }
      function ensureVideoCaptionListeners() {
        const video = findPlayerVideo();
        if (!video || videosWithCaptionListeners.has(video)) return;
        videosWithCaptionListeners.add(video);
        const sync = () => staggeredScheduleProcess();
        video.addEventListener("pause", sync);
        video.addEventListener("play", sync);
        video.addEventListener("seeking", sync);
        video.addEventListener("seeked", sync);
        video.addEventListener("loadeddata", sync);
        video.addEventListener("loadedmetadata", sync);
      }
      function ensureBudouxToggleStyles() {
        let el = document.getElementById(BUDOUX_TOGGLE_STYLE_ID);
        if (!el) {
          el = document.createElement("style");
          el.id = BUDOUX_TOGGLE_STYLE_ID;
          (document.head ?? document.documentElement).appendChild(el);
        }
        el.textContent = `
.${CLS_HOST} {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  align-self: stretch;
  flex-shrink: 0;
  margin: 0 4px 0 2px;
  min-height: 0;
  padding: 6px 8px;
  cursor: pointer;
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}
/* \u6642\u8A08\u30A2\u30D7\u30EA\u306E\u30BF\u30D6\u9078\u629E\u306E\u3088\u3046\u306A\u3001\u89D2\u306E\u53D6\u308C\u305F\u30AB\u30D7\u30BB\u30EB\uFF08\u30DB\u30D0\u30FC\u30FB\u30BB\u30B0\u30E1\u30F3\u30C8\uFF09 */
.${CLS_HOST}::before {
  content: "";
  position: absolute;
  left: 1px;
  right: 1px;
  top: 50%;
  transform: translateY(-50%);
  height: 32px;
  border-radius: 9999px;
  background: rgba(255, 255, 255, 0.13);
  opacity: 0;
  transition: opacity 0.2s ease, background 0.2s ease;
  pointer-events: none;
  z-index: 0;
}
.${CLS_HOST}:hover::before {
  opacity: 1;
  background: rgba(255, 255, 255, 0.18);
}
/* document.body \u76F4\u4E0B\u3002\u30D7\u30EC\u30FC\u30E4\u30FC\u306E overflow \u3067\u5207\u3089\u308C\u306A\u3044 */
#${BODY_TOOLTIP_ID}.${CLS_TOOLTIP} {
  position: fixed;
  z-index: 2147483646;
  padding: 10px 14px;
  border-radius: 4px;
  background: rgba(28, 28, 28, 0.96);
  color: #fff;
  font-size: 12px;
  line-height: 1.35;
  font-family: Roboto, "YouTube Noto", Arial, sans-serif;
  text-align: center;
  max-width: min(320px, 80vw);
  white-space: normal;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.45);
  pointer-events: none;
}
.ytp-budoux-jwbreak-tooltip-name {
  display: block;
  font-weight: 500;
  font-size: 12px;
  margin-bottom: 4px;
  letter-spacing: 0.01em;
}
.ytp-budoux-jwbreak-tooltip-desc {
  display: block;
  font-size: 11px;
  font-weight: 400;
  opacity: 0.88;
  line-height: 1.35;
}
.ytp-budoux-jwbreak-tooltip-shortcut {
  display: block;
  margin-top: 6px;
  padding-top: 6px;
  border-top: 1px solid rgba(255, 255, 255, 0.12);
  font-size: 10px;
  font-weight: 400;
  opacity: 0.85;
  line-height: 1.35;
}
/* \u30B3\u30F3\u30C8\u30ED\u30FC\u30EB\u30D0\u30FC\u4E0A\u306E\u30C8\u30B0\u30EB\uFF08\u30A2\u30A4\u30B3\u30F3\u62E1\u5927\u306B\u5408\u308F\u305B\u3066\u3084\u3084\u5927\u578B\uFF09 */
.${CLS_SWITCH} {
  position: relative;
  z-index: 1;
  width: 48px;
  height: 22px;
  border-radius: 11px;
  flex-shrink: 0;
  transition: background 0.15s ease, box-shadow 0.15s ease;
  box-sizing: border-box;
}
.${CLS_SWITCH}[aria-checked="false"] {
  background: rgba(255, 255, 255, 0.28);
  box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.25);
}
.${CLS_SWITCH}[aria-checked="true"] {
  background: #6d28d9;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.22),
    0 0 0 1px rgba(0, 0, 0, 0.12);
}
.${CLS_KNOB} {
  position: absolute;
  top: 3px;
  left: 3px;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.18s ease, background 0.15s ease;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.32);
}
.${CLS_KNOB} .${CLS_KNOB_ICON} {
  display: block;
  width: 14px;
  height: 14px;
  color: #fff;
  opacity: 0;
  transition: opacity 0.12s ease;
  flex-shrink: 0;
  pointer-events: none;
}
.${CLS_SWITCH}[aria-checked="true"] .${CLS_KNOB} .${CLS_KNOB_ICON} {
  opacity: 1;
}
.${CLS_SWITCH}[aria-checked="false"] .${CLS_KNOB} {
  background: #fff;
}
.${CLS_SWITCH}[aria-checked="true"] .${CLS_KNOB} {
  background: ${KNOB_ON_FACE};
  transform: translateX(26px);
}
.${CLS_SWITCH}:focus {
  outline: none;
}
.${CLS_SWITCH}:focus-visible {
  outline: 2px solid rgba(255, 255, 255, 0.95);
  outline-offset: 2px;
}
.ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"],
.ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] {
  left: 50% !important;
  right: auto !important;
  width: fit-content !important;
  max-width: calc(100% - 48px) !important;
  min-width: 0 !important;
  margin-left: 0 !important;
  margin-right: 0 !important;
  padding-left: 0 !important;
  padding-right: 0 !important;
  box-sizing: border-box !important;
  text-align: center !important;
  transform: translateX(-50%) !important;
}
.ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .caption-visual-line,
.ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .caption-visual-line {
  opacity: 1;
  animation: yt-budoux-caption-fade-in 180ms ease-out both;
}
.ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] > .ytp-caption-segment,
.ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] > .ytp-caption-segment,
.ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .ytp-caption-window-inner > .ytp-caption-segment,
.ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .ytp-caption-window-inner > .ytp-caption-segment,
.ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .captions-text > .ytp-caption-segment,
.ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .captions-text > .ytp-caption-segment {
  opacity: 1;
  animation: yt-budoux-caption-fade-in 180ms ease-out both;
}
@keyframes yt-budoux-caption-fade-in {
  from {
    opacity: 0;
    transform: translateY(4px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
@media (prefers-reduced-motion: reduce) {
  .ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .caption-visual-line,
  .ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .caption-visual-line,
  .ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] > .ytp-caption-segment,
  .ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] > .ytp-caption-segment,
  .ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .ytp-caption-window-inner > .ytp-caption-segment,
  .ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .ytp-caption-window-inner > .ytp-caption-segment,
  .ytp-caption-window-container .ytp-caption-window[${ATTR_DONE}="1"] .captions-text > .ytp-caption-segment,
  .ytp-caption-window-container .caption-window[${ATTR_DONE}="1"] .captions-text > .ytp-caption-segment {
    animation: none;
  }
}
`;
      }
      function escapeHtml(s) {
        return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
      }
      function updateTooltipContent(tip) {
        const on = budouxCaptionEnabled;
        const baseDetail = on ? "\u8AAD\u307F\u3084\u3059\u3044\u4F4D\u7F6E\u3067\u6539\u884C\u3057\u3066\u3044\u307E\u3059\uFF08\u30AF\u30EA\u30C3\u30AF\u3067\u5143\u306E\u5B57\u5E55\u3069\u304A\u308A\uFF09" : "YouTube \u306E\u5143\u306E\u6539\u884C\u3069\u304A\u308A\u3067\u3059\uFF08\u30AF\u30EA\u30C3\u30AF\u3067\u8AAD\u307F\u3084\u3059\u304F\u6574\u5F62\uFF09";
        const shortcutLine = cachedToggleShortcut
          ? `\u30B7\u30E7\u30FC\u30C8\u30AB\u30C3\u30C8: ${escapeHtml(cachedToggleShortcut)}`
          : "\u30B7\u30E7\u30FC\u30C8\u30AB\u30C3\u30C8: \u672A\u8A2D\u5B9A\uFF08\u62E1\u5F35\u6A5F\u80FD \u003E \u30AD\u30FC\u30DC\u30FC\u30C9\u30B7\u30E7\u30FC\u30C8\u30AB\u30C3\u30C8\u3067\u5909\u66F4\uFF09";
        tip.innerHTML = `<span class="ytp-budoux-jwbreak-tooltip-name">${escapeHtml(UI_TOGGLE_LABEL)}</span><span class="ytp-budoux-jwbreak-tooltip-desc">${escapeHtml(baseDetail)}</span><span class="ytp-budoux-jwbreak-tooltip-shortcut">${shortcutLine}</span>`;
      }
      function updateToggleSwitchAppearance(host, sw) {
        const on = budouxCaptionEnabled;
        sw.setAttribute("aria-checked", on ? "true" : "false");
        sw.setAttribute("aria-label", on ? `${UI_TOGGLE_LABEL}\uFF1A\u30AA\u30F3` : `${UI_TOGGLE_LABEL}\uFF1A\u30AA\u30D5\uFF08\u539F\u6587\u3069\u304A\u308A\uFF09`);
        host.removeAttribute("title");
        sw.removeAttribute("title");
        const tip = document.getElementById(BODY_TOOLTIP_ID);
        if (tooltipAnchorHost === host && tip && tip.style.display !== "none" && tip.style.opacity !== "0") {
          updateTooltipContent(tip);
          layoutBodyTooltipNearHost(host);
        }
      }
      function getBodyTooltipEl() {
        let tip = document.getElementById(BODY_TOOLTIP_ID);
        if (!tip) {
          tip = document.createElement("div");
          tip.id = BODY_TOOLTIP_ID;
          tip.className = CLS_TOOLTIP;
          tip.setAttribute("role", "tooltip");
          tip.style.display = "none";
          tip.style.opacity = "0";
          tip.style.visibility = "hidden";
          document.body.appendChild(tip);
        }
        return tip;
      }
      function layoutBodyTooltipNearHost(host) {
        const tip = getBodyTooltipEl();
        const r = host.getBoundingClientRect();
        const tw = tip.offsetWidth || 240;
        const th = tip.offsetHeight || 52;
        let left = r.left + r.width / 2 - tw / 2;
        left = Math.max(10, Math.min(left, window.innerWidth - tw - 10));
        let top = r.bottom + 12;
        if (top + th > window.innerHeight - 8)
          top = r.top - th - 12;
        if (top < 8) top = 8;
        tip.style.left = `${left}px`;
        tip.style.top = `${top}px`;
      }
      function bindGlobalTooltipLayoutSync() {
        const g = globalThis;
        if (g.__ytBudouxTipLayout) return;
        g.__ytBudouxTipLayout = true;
        const sync = () => {
          if (tooltipAnchorHost) layoutBodyTooltipNearHost(tooltipAnchorHost);
        };
        window.addEventListener("scroll", sync, true);
        window.addEventListener("resize", sync);
      }
      function showBodyTooltip(host) {
        tooltipAnchorHost = host;
        bindGlobalTooltipLayoutSync();
        fetchToggleShortcut(() => {
          const tip = getBodyTooltipEl();
          updateTooltipContent(tip);
          tip.style.display = "block";
          tip.style.visibility = "visible";
          tip.style.opacity = "1";
          requestAnimationFrame(() => {
            layoutBodyTooltipNearHost(host);
            requestAnimationFrame(() => layoutBodyTooltipNearHost(host));
          });
        });
      }
      function hideBodyTooltip() {
        tooltipAnchorHost = null;
        const tip = document.getElementById(BODY_TOOLTIP_ID);
        if (!tip) return;
        tip.style.opacity = "0";
        tip.style.visibility = "hidden";
        tip.style.display = "none";
      }
      function createLineBreakIconSvg() {
        const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
        svg.setAttribute("class", CLS_KNOB_ICON);
        svg.setAttribute("xmlns", "http://www.w3.org/2000/svg");
        svg.setAttribute("fill", "none");
        svg.setAttribute("viewBox", "0 0 14 14");
        svg.setAttribute("width", "14");
        svg.setAttribute("height", "14");
        svg.setAttribute("aria-hidden", "true");
        svg.setAttribute("focusable", "false");
        const path = document.createElementNS("http://www.w3.org/2000/svg", "path");
        path.setAttribute(
          "d",
          "M12 2V8H4.414L6.707 10.293L5.293 11.707L0.586 7L5.293 2.293L6.707 3.707L4.414 6H10V2H12Z"
        );
        path.setAttribute("fill", "currentColor");
        svg.appendChild(path);
        return svg;
      }
      function ensureKnobInSwitch(sw) {
        let k = sw.querySelector(`.${CLS_KNOB}`);
        if (!k) {
          k = document.createElement("span");
          k.className = CLS_KNOB;
          sw.appendChild(k);
        }
        if (!k.querySelector(`.${CLS_KNOB_ICON}`)) {
          k.appendChild(createLineBreakIconSvg());
        }
      }
      function ensureTooltipHover(host) {
        if (host.dataset.ytBudouxTooltipHoverBound === "1") return;
        host.dataset.ytBudouxTooltipHoverBound = "1";
        bindGlobalTooltipLayoutSync();
        let showTimer = null;
        let hideTimer = null;
        host.addEventListener("mouseenter", () => {
          if (hideTimer !== null) {
            clearTimeout(hideTimer);
            hideTimer = null;
          }
          if (showTimer !== null) clearTimeout(showTimer);
          showTimer = setTimeout(() => {
            showTimer = null;
            showBodyTooltip(host);
          }, 220);
        });
        host.addEventListener("mouseleave", () => {
          if (showTimer !== null) {
            clearTimeout(showTimer);
            showTimer = null;
          }
          if (hideTimer !== null) clearTimeout(hideTimer);
          hideTimer = setTimeout(() => {
            hideTimer = null;
            hideBodyTooltip();
          }, 80);
        });
        host.addEventListener("focusin", () => {
          if (showTimer !== null) {
            clearTimeout(showTimer);
            showTimer = null;
          }
          showBodyTooltip(host);
        });
        host.addEventListener("focusout", () => {
          hideBodyTooltip();
        });
      }
      function bindBudouxSwitch(host, sw) {
        const activate = () => {
          saveBudouxEnabled(!budouxCaptionEnabled);
          updateToggleSwitchAppearance(host, sw);
          processCaptionsNow();
        };
        host.addEventListener("click", (e) => {
          e.preventDefault();
          e.stopPropagation();
          activate();
        });
        sw.addEventListener("keydown", (e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            e.stopPropagation();
            activate();
          }
        });
      }
      function placeToggleHost(host) {
        const sub = queryInMoviePlayer(".ytp-subtitles-button");
        if (sub?.parentElement) {
          const p = sub.parentElement;
          if (sub.nextSibling !== host || host.parentElement !== p) {
            p.insertBefore(host, sub.nextSibling);
          }
          return;
        }
        const rc = queryInMoviePlayer(".ytp-right-controls");
        if (!rc) return;
        const autonav = rc.querySelector(".ytp-autonav-toggle-button-container");
        if (autonav?.parentNode === rc) {
          if (autonav.nextSibling !== host || host.parentElement !== rc) {
            rc.insertBefore(host, autonav.nextSibling);
          }
          return;
        }
        if (host.parentNode !== rc) rc.appendChild(host);
      }
      function migrateHostToSwitchIfNeeded(host) {
        host.className = CLS_HOST;
        host.style.cssText = "";
        host.querySelectorAll(`.${CLS_TOOLTIP}`).forEach((n) => n.remove());
        let sw = host.querySelector("[data-yt-budoux-switch]");
        if (!sw) {
          host.replaceChildren();
          sw = document.createElement("div");
          sw.className = CLS_SWITCH;
          sw.setAttribute("data-yt-budoux-switch", "");
          sw.setAttribute("role", "switch");
          sw.tabIndex = 0;
          ensureKnobInSwitch(sw);
          host.appendChild(sw);
          bindBudouxSwitch(host, sw);
        } else {
          sw.className = CLS_SWITCH;
          ensureKnobInSwitch(sw);
        }
        return sw;
      }
      function toggleBudouxCaptionFromHotkey() {
        try {
          saveBudouxEnabled(!budouxCaptionEnabled);
          ensureToggleInjected();
          const host = queryInMoviePlayer(`#${TOGGLE_HOST_ID}`);
          const sw = host instanceof HTMLElement ? host.querySelector("[data-yt-budoux-switch]") : null;
          if (host && sw) updateToggleSwitchAppearance(host, sw);
          processCaptionsNow();
        } catch {
        }
      }
      chrome.runtime.onMessage.addListener((msg, _sender, respond) => {
        if (msg && msg.type === "BUDOUX_TOGGLE_HOTKEY") {
          toggleBudouxCaptionFromHotkey();
          respond({ ok: true });
          return true;
        }
        return void 0;
      });
      function ensureToggleInjected() {
        ensureBudouxToggleStyles();
        if (!queryInMoviePlayer(".ytp-right-controls") && !queryInMoviePlayer(".ytp-subtitles-button")) return;
        const existingHost = queryInMoviePlayer(`#${TOGGLE_HOST_ID}`);
        if (existingHost) {
          const sw2 = migrateHostToSwitchIfNeeded(existingHost);
          updateToggleSwitchAppearance(existingHost, sw2);
          placeToggleHost(existingHost);
          ensureTooltipHover(existingHost);
          return;
        }
        const host = document.createElement("div");
        host.id = TOGGLE_HOST_ID;
        const sw = migrateHostToSwitchIfNeeded(host);
        updateToggleSwitchAppearance(host, sw);
        placeToggleHost(host);
        ensureTooltipHover(host);
      }
      function isPrimarilyJapanese(text) {
        const noSpace = text.replace(/\s/g, "");
        if (!noSpace) return false;
        const jp = noSpace.match(
          /[\u3000-\u303f\u3040-\u309f\u30a0-\uff9f\u3400-\u4dbf\u4e00-\u9faf]/g
        );
        return !!jp && jp.length >= noSpace.length * 0.25;
      }
      function normalizeJpCaptionLine(text) {
        return text
          .replace(/\r/g, "")
          .replace(/\n/g, " ")
          .replace(/\s+/g, " ")
          .replace(/\u3057\u307e[\s　]+\u305b\u3093/gu, "\u3057\u307e\u305b\u3093")
          .replace(/\u3057([\u307e\u307E]\u305B)[\s　]+\u3093/gu, "\u3057$1\u3093")
          .replace(/\u3093[\s　]+(?=\u3068\u306b\u304b\u304f)/gu, "\u3093")
          .replace(/\u306E[\s　]+\u3067(?=[、，])/gu, "\u306E\u3067")
          .replace(/ないので\s+す(?=[\u2026\u22EF⋯…．。\s　!！?？]|(?:\.{2,}))/gu, "ないのです")
          .replace(/助かり\s+ます/gu, "助かります")
          .trim();
      }
      function hintFlatForJpCaptionParsing(flat) {
        let s = normalizeJpCaptionLine(flat);
        s = s.replace(/\u3057\u307e[\s　]+\u305b\u3093/gu, "\u3057\u307e\u305b\u3093");
        s = s.replace(/\u3057([\u307e\u307E]\u305B)[\s　]+\u3093/gu, "\u3057$1\u3093");
        s = s.replace(/ないので\s+す(?=[\u2026\u22EF…．。\s　!！?？]|(?:\.{2,}))/gu, "ないのです");
        s = s.replace(/助かり\s+ます/gu, "助かります");
        s = s.replace(/(タイトル)(?=正直)/gu, "$1\u3001");
        s = s.replace(/しませんでした(?=とにかく)/gu, "しませんでした\u3002");
        s = s.replace(/しません(?=とにかく)/gu, "しません\u3002");
        s = s.replace(/しました(?=とにかく)/gu, "しました\u3002");
        s = s.replace(/でした(?=とにかく)/gu, "でした\u3002");
        return s;
      }
      function parseJpCaptionChunks(flatDisplay) {
        return parser.parse(hintFlatForJpCaptionParsing(normalizeJpCaptionLine(flatDisplay)));
      }
      function jpCaptionLineBreakpointDisplay(joinedChunks, otherJoinedChunks) {
        let s = normalizeJpCaptionLine(joinedChunks);
        const headOther = otherJoinedChunks !== null ? normalizeJpCaptionLine(otherJoinedChunks).slice(0, 3) : "";
        if (/タイトル、$/.test(s) && headOther.startsWith("\u6B63\u76F4"))
          return s.replace(/タイトル、$/u, "\u30BF\u30A4\u30C8\u30EB");
        return s;
      }
      function jpCaptionSyntheticHintStripped(joinedChunks) {
        return normalizeJpCaptionLine(joinedChunks).replace(/タイトル、(?=正直)/gu, "\u30BF\u30A4\u30C8\u30EB");
      }
      function extractCaptionVisualLineTexts(contentRoot) {
        const visuals = Array.from(contentRoot.querySelectorAll(".caption-visual-line"));
        if (visuals.length > 0) {
          const out = [];
          for (const vl of visuals) {
            const segs = vl.querySelectorAll(".ytp-caption-segment");
            const t = segs.length > 0 ? Array.from(segs).map((s) => (s.textContent ?? "").trim()).join("") : normalizeJpCaptionLine(vl.textContent ?? "");
            const n = normalizeJpCaptionLine(t);
            if (n.length > 0) out.push(n);
          }
          return out;
        }
        const segments = Array.from(contentRoot.querySelectorAll(".ytp-caption-segment"));
        if (segments.length > 0) {
          const joined = normalizeJpCaptionLine(
            segments.map((s) => s.textContent ?? "").join("")
          );
          return joined.length > 0 ? [joined] : [];
        }
        const one = normalizeJpCaptionLine(contentRoot.textContent ?? "");
        return one.length > 0 ? [one] : [];
      }
      function collectCaptionText(root) {
        const segments = root.querySelectorAll(".ytp-caption-segment");
        if (segments.length > 0) {
          return Array.from(segments).map((s) => s.textContent ?? "").join("").replace(/\r/g, "").replace(/\n/g, " ").replace(/\s+/g, " ").trim();
        }
        const lines = root.querySelectorAll(".caption-visual-line");
        if (lines.length > 0) {
          return Array.from(lines).map((l) => (l.textContent ?? "").trim()).filter(Boolean).join(" ").replace(/\s+/g, " ").trim();
        }
        return (root.textContent ?? "").replace(/\s+/g, " ").trim();
      }
      function isOrphanPunctuationOnlyLine(line) {
        const t = normalizeJpCaptionLine(line);
        if (!t.length) return false;
        return !/[\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Han}A-Za-z0-9]/u.test(t);
      }
      function isWeakBreakBeforeParticleLineStart(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (!/^(に|の|を|が|も|へ|と|で|て|から|まで|より)(?=[ぁ-んァ-ヶ一-龯々〆〻゛゜])/u.test(
          b
        ))
          return false;
        const last = a.slice(-1);
        if (/[、。．！？…」』,，]$/.test(a)) return false;
        if (/[はがをのにでとへもやかよね]$/.test(last)) return false;
        if (/[いてたれろんっく]$/.test(last)) return false;
        return true;
      }
      function splitsLocative\u3067BeforeGerundEtc(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．!！?？]$/.test(a)) return false;
        if (!/で$/u.test(a)) return false;
        return /^(磨いて|読んで|読み|書いて|話して|見て|聞いて|座って|歩いて|並んで|叩いて|並び|並べ|済んで|済ませ|使って|しまって|しまい|伝え|伝わ|伝っ|伝えて)/u.test(
          b
        );
      }
      function splits\u304CBeforePredicateContinuation(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．]$/.test(a)) return false;
        if (!/が$/u.test(a)) return false;
        return /^(無い|ない|なかった|あった|なる|のみ|とは|だった|であった)/u.test(b);
      }
      function splitsPolitePastBefore\u305F(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．]$/.test(a)) return false;
        return (/まし$/u.test(a) || /でし$/u.test(a)) && /^た/u.test(b);
      }
      function splitsCompoundVerbOrNounAcrossLines(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．]$/.test(a)) return false;
        return /加$/u.test(a) && /^入/u.test(b) || /予$/u.test(a) && /^約/u.test(b);
      }
      function splitsTopic\u306FBeforeContinuingClause(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．]$/.test(a)) return false;
        if (!/は$/u.test(a)) return false;
        return /^(既に|もう|まだ|ただ|すでに|すぐ|たった)/u.test(b);
      }
      function splitsShortRenyoCommaBeforeHeavyContinuation(line1, line2) {
        const a = normalizeJpCaptionLine(line1).replace(/\s+$/u, "");
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (!/[、，]$/.test(a)) return false;
        if (/して、[、，]*$/u.test(a)) return false;
        if (a.length > 17) return false;
        if (b.length < 36) return false;
        return /(し|て)、$/.test(a);
      }
      function splits\u3068\u304BBefore\u3042\u308C\u3070Continuation(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[，、。．]$/.test(a)) return false;
        if (!/とか$/u.test(a)) return false;
        return /^あれば/u.test(b);
      }
      function splitsContrastiveTransitionCommaLeavesTopicHaOnUpperLine(line1, line2) {
        const a = normalizeJpCaptionLine(line1);
        const b = normalizeJpCaptionLine(line2);
        if (!a.length || !b.length) return false;
        if (/[。.．!！?？」』][\s　]*$/u.test(a)) return false;
        if (
          !/(ですが|けれども?|だけど|だが|とはいえ|それでも)[、，]/u.test(a)
        )
          return false;
        return /(?:今日は|明日は|今は|今回は|本日は)\s*$/u.test(a);
      }
      function subtitlePauseBreakpointBonus(joinedLeft, joinedRight) {
        const s = jpCaptionSyntheticHintStripped(
          jpCaptionLineBreakpointDisplay(joinedLeft, joinedRight)
        ).trimEnd();
        const hintRight = jpCaptionSyntheticHintStripped(joinedRight);
        const headRight = hintRight.slice(0, 3);
        if (/タイトル$/.test(s) && headRight.startsWith("\u6B63\u76F4")) return 21;
        if (/[\u3002\uFF0E][\s\u2060]*$/u.test(s) && /^[\s\u2060]*\u3068\u306b\u304b\u304f/u.test(hintRight)) return 43;
        const otherLen = [...hintRight].length;
        if (/ので[、，]$/.test(s)) return 38;
        if (/が[、，]$/.test(s)) {
          if (otherLen >= 22) return 38;
          if (/ですが[、，]$|だけど[、，]$|けれども?[、，]$|だが[、，]$/u.test(s) && otherLen >= 8)
            return 40;
          if (otherLen < 6) return 0;
          if ([...s].length > 28) return 18;
          return 6;
        }
        if (/までなら$/u.test(s)) return 34;
        if (/あれば$/u.test(s)) return 30;
        if (/[，、]$/.test(s)) return 22;
        if (/今更感は$/.test(s)) return 4;
        if (/[\p{L}\p{N}\u30FC]+版は$/u.test(s) && !/(では|とは)$/.test(s)) return 28;
        if (/は$/u.test(s) && !/(では|とは|からは|には)$/.test(s)) return 12;
        return 0;
      }
      function splitsLeadingTopicHaOnlyBeforeContrastiveClause(line1, line2) {
        const a = normalizeJpCaptionLine(line1).replace(/\u2060/g, "");
        const b = normalizeJpCaptionLine(line2).replace(/\u2060/g, "");
        if (!a.length || !b.length) return false;
        if (!/^(昨日は|今は|今日は|明日は|先日は|本日は|今朝は|昨夜は|今回は)([、，])?$/u.test(a)) return false;
        return /(?:ですが|ってますが)[、，]/u.test(b) || /(けれども?|だけど|だが|とはいえ|それでも)[、，]/u.test(b);
      }
      function computeTwoLineSplit(chunks) {
        const flatDisp = jpCaptionSyntheticHintStripped(chunks.join(""));
        if (!flatDisp) return [];
        if (chunks.length <= 1) {
          return [flatDisp];
        }
        if (flatDisp.length <= 22) return [flatDisp];
        const totalChars = flatDisp.length;
        const target = totalChars / 2;
        const candidates = [];
        for (let i = 0; i < chunks.length - 1; i++) {
          const joinedLeft = chunks.slice(0, i + 1).join("");
          const joinedRight = chunks.slice(i + 1).join("");
          const leftDisp = jpCaptionSyntheticHintStripped(
            jpCaptionLineBreakpointDisplay(joinedLeft, joinedRight)
          );
          const run = leftDisp.length;
          candidates.push({ i, run, diff: Math.abs(run - target) });
        }
        const bestDiff = Math.min(...candidates.map((c) => c.diff));
        const slack = Math.max(4, Math.floor(totalChars * 0.08));
        const inBand = candidates.filter((c) => c.diff <= bestDiff + slack);
        const outOfBand = candidates.filter((c) => c.diff > bestDiff + slack);
        const orderIn = inBand.slice().sort((a, b) => a.diff - b.diff || b.run - a.run);
        const orderOut = outOfBand.slice().sort((a, b) => a.diff - b.diff || b.run - a.run);
        const tentative = orderIn.length > 0 ? [...orderIn, ...orderOut] : orderOut;
        tentative.sort((a, b) => {
          const restAJoin = chunks.slice(a.i + 1).join("");
          const restBJoin = chunks.slice(b.i + 1).join("");
          const l1aJoin = chunks.slice(0, a.i + 1).join("");
          const l1bJoin = chunks.slice(0, b.i + 1).join("");
          const rankA = a.diff - subtitlePauseBreakpointBonus(l1aJoin, restAJoin) * 4;
          const rankB = b.diff - subtitlePauseBreakpointBonus(l1bJoin, restBJoin) * 4;
          if (rankA !== rankB) return rankA - rankB;
          return b.run - a.run;
        });
        for (const chosen of tentative) {
          const joined1 = chunks.slice(0, chosen.i + 1).join("");
          const joined2 = chunks.slice(chosen.i + 1).join("");
          const L1 = jpCaptionSyntheticHintStripped(jpCaptionLineBreakpointDisplay(joined1, joined2));
          const L2 = jpCaptionSyntheticHintStripped(joined2);
          if (!L2) return [L1];
          if (isOrphanPunctuationOnlyLine(L2)) continue;
          if (isWeakBreakBeforeParticleLineStart(L1, L2)) continue;
          if (splitsTopic\u306FBeforeContinuingClause(L1, L2)) continue;
          if (splitsShortRenyoCommaBeforeHeavyContinuation(L1, L2)) continue;
          if (splits\u3068\u304BBefore\u3042\u308C\u3070Continuation(L1, L2)) continue;
          if (splitsLocative\u3067BeforeGerundEtc(L1, L2)) continue;
          if (splits\u304CBeforePredicateContinuation(L1, L2)) continue;
          if (splitsPolitePastBefore\u305F(L1, L2)) continue;
          if (splitsCompoundVerbOrNounAcrossLines(L1, L2)) continue;
          if (splitsContrastiveTransitionCommaLeavesTopicHaOnUpperLine(L1, L2)) continue;
          if (splitsLeadingTopicHaOnlyBeforeContrastiveClause(L1, L2)) continue;
          const l1Trim = normalizeJpCaptionLine(L1).replace(/\u2060/g, "");
          const l2Trim = normalizeJpCaptionLine(L2).replace(/\u2060/g, "");
          if (/\u3086\u3063\u304F$/u.test(l1Trim) && /^\u308A/u.test(l2Trim)) continue;
          if (/しま$/u.test(l1Trim) && /^\u305b\u3093/u.test(l2Trim)) continue;
          if (/[\u307E\u307e]\u305B$/u.test(l1Trim) && /^\u3093/u.test(l2Trim)) continue;
          if (/ま$/u.test(l1Trim) && /^せん/u.test(l2Trim)) continue;
          if (/で$/u.test(l1Trim) && /^\u3059/u.test(l2Trim)) continue;
          if (/の$/u.test(l1Trim) && /^で[、，]/u.test(l2Trim)) continue;
          return [L1, L2];
        }
        return [flatDisp];
      }
      function subtitleRestBenefitsThirdLine(restLine) {
        const R = normalizeJpCaptionLine(restLine);
        if (!R.length) return false;
        if (R.length > 42) return true;
        const mSo = /ので[，、]([\s\S]*)$/u.exec(R);
        if (mSo) {
          const tail = normalizeJpCaptionLine(mSo[1]).replace(/[。．]+$/, "");
          if (tail.length >= 6) return true;
        }
        const mGa = /が[，、]([\s\S]*)$/u.exec(R);
        if (mGa) {
          const tail = normalizeJpCaptionLine(mGa[1]).replace(/[。．]+$/, "");
          if (tail.length >= 8) return true;
        }
        if (/\u3068\u306b\u304b\u304f/u.test(R) && [...R].length >= 12) return true;
        return false;
      }
      function remainderWarrantsCaptionFourthLine(restLine, wholeFlatDisp) {
        const R = normalizeJpCaptionLine(restLine);
        if (!R.length) return false;
        if (wholeFlatDisp.length < 62) return false;
        if (R.length >= 46) return true;
        if (subtitleRestBenefitsThirdLine(R)) return true;
        return R.length > 38;
      }
      function balanceLinesUpToMax(chunks, maxLines) {
        const pair = computeTwoLineSplit(chunks);
        const flatDisp = jpCaptionSyntheticHintStripped(chunks.join(""));
        const allowShortFlatThirdLine =
          maxLines >= 3 &&
          /\u3068\u306b\u304b\u304f/u.test(flatDisp) &&
          flatDisp.length < MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE;
        if (
          (flatDisp.length < MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE && !allowShortFlatThirdLine) ||
          pair.length <= 1 ||
          maxLines <= 2
        ) {
          return pair.map((s) => normalizeJpCaptionLine(s));
        }
        const [line1, rest1] = pair;
        const L1 = normalizeJpCaptionLine(line1);
        const rBody = normalizeJpCaptionLine(rest1);
        const gate3 = rBody.length && rBody.length >= 14 && (subtitleRestBenefitsThirdLine(rBody) || rBody.length > 38);
        if (!gate3 || maxLines < 3) {
          return [L1, rBody].filter(Boolean);
        }
        const restChunks = parseJpCaptionChunks(rBody);
        if (!restChunks || restChunks.length <= 1) {
          return [L1, rBody].filter(Boolean);
        }
        const subPair = computeTwoLineSplit(restChunks);
        if (subPair.length <= 1) {
          return [L1, rBody].filter(Boolean);
        }
        const L2 = normalizeJpCaptionLine(subPair[0] ?? "");
        const rr = normalizeJpCaptionLine(subPair[1] ?? "");
        const three = [L1, L2, rr].filter(Boolean);
        if (maxLines < 4 || flatDisp.length < 62 || !remainderWarrantsCaptionFourthLine(rr, flatDisp)) {
          return three;
        }
        const restChunks2 = parseJpCaptionChunks(rr);
        if (!restChunks2 || restChunks2.length <= 1) return three;
        const subPair2 = computeTwoLineSplit(restChunks2);
        if (subPair2.length <= 1) return three;
        return [
          L1,
          L2,
          normalizeJpCaptionLine(subPair2[0] ?? ""),
          normalizeJpCaptionLine(subPair2[1] ?? "")
        ].filter(Boolean);
      }
      function maxCaptionLinesForFlatDisplay(flatCanon) {
        const f = normalizeJpCaptionLine(flatCanon);
        const cc = [...f].length;
        let mx = cc >= 62 ? 4 : cc >= MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE ? 3 : 2;
        if (cc >= 40 && /\u3068\u306b\u304b\u304f/u.test(f)) mx = Math.max(mx, 3);
        return mx;
      }
      function maxCaptionLinesForTextLength(chars) {
        if (chars >= 62) return 4;
        if (chars >= MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE) return 3;
        return 2;
      }
      function balanceLines(chunks, maxLines) {
        return balanceLinesUpToMax(chunks, maxLines);
      }
      function hashText(s) {
        const n = s.replace(/\u2060/g, "");
        let h = 0;
        for (let i = 0; i < n.length; i++) {
          h = Math.imul(31, h) + n.charCodeAt(i) | 0;
        }
        return String(h);
      }
      var WORD_JOINER = "\u2060";
      /**
       * ひらがな語のつながりが読みにくい箇所に半角スペースを入れる（誤爆抑止のホワイトリスト中心）。
       * 例: ブログ等で挙がる「私は はてな」のように助詞「は」のあとにひらがな語が続くところへの区切り。
       * Web上の「半角記号・英字と全角の間に半角スペース」（asiamoth.com 等に言及あり）も短い英字列向けに適用。
       */
      function captionStripCueBreakDotsAfterPolitenessCue(line) {
        let t = normalizeJpCaptionLine(line);
        return t.replace(
          /(?:しません(?:でした)?|しました|できません(?:でした)?|でした)\u3002\s*$/u,
          (frag) => frag.replace(/\u3002\s*$/u, "")
        ).trimEnd();
      }
      function readableJpCaptionInsertHalfwidthSpaces(line) {
        let s = normalizeJpCaptionLine(line);
        if (!s.length || !/[\u3041-\u3096]/.test(s)) return s;
        const H = "[\\u3041-\\u3096]";
        const KK = "[\\u30A1-\\u30FC\\uFF66-\\uFF9F]";
        const spacedAfterTopicHa = new RegExp(
          `(?:今日は|明日は|昨日は|昨夜は|今朝は|今夜は|今週は|今月は|今年は|来年は|本日は|先日は|実は|今回は|前回は|次回は|毎回は|初回は|こんにちは|おはようございます|おやすみなさい|ありがとうございます|ありがとうございました|ここは|そこは|あそこは|私は|僕は|俺は|自分は|二人は|三人は|みんなは)(?=${H})(?! )`,
          "gu"
        );
        s = s.replace(spacedAfterTopicHa, (w) => `${w} `);
        s = s.replace(
          new RegExp(`(?:今は)(?=${H})(?! )(?!日)`, "gu"),
          "今は "
        );
        s = s.replace(
          new RegExp(`(${KK}{2,})([をはにがもでへ])(?=${H})(?! )`, "gu"),
          "$1 $2"
        );
        s = s.replace(
          /([A-Za-z][A-Za-z0-9._%+-]{2,})(?=[^をはがもでにへと])(?=[\u3041-\u3096])(?! )/gu,
          "$1 "
        );
        return s.replace(/ {2,}/gu, " ").trimEnd();
      }
      function glueInseparableJpCompounds(line) {
        return line.replace(/加入/g, `\u52A0${WORD_JOINER}\u5165`).replace(
          /\u3086\u3063\u304F\u308A/g,
          `\u3086\u3063\u304F${WORD_JOINER}\u308A`
        );
      }
      function looksLikeLongCuePlusShortTailLine(main, tail) {
        const a = normalizeJpCaptionLine(main);
        const b = normalizeJpCaptionLine(tail);
        if (!a.length || !b.length) return false;
        if (!(isPrimarilyJapanese(a) && isPrimarilyJapanese(b))) return false;
        return a.length >= 28 && b.length <= 32 && b.length * 100 <= a.length * 72;
      }
      function shouldPreserveNativeTwoLineSignoff(cue1, cue2) {
        const a = normalizeJpCaptionLine(cue1);
        const b = normalizeJpCaptionLine(cue2);
        if (!a.length || !b.length) return false;
        if (!(isPrimarilyJapanese(a) && isPrimarilyJapanese(b))) return false;
        if (a.length > 56 || b.length > 56) return false;
        if (!/ここまで[。．!！?？\s　]*$/u.test(a)) return false;
        return /ご視聴|御視聴/u.test(b) && /ありがとう/u.test(b);
      }
      function shouldPreserveNativeTwoLineParagraphBoundary(cue1, cue2) {
        const a = normalizeJpCaptionLine(cue1);
        const b = normalizeJpCaptionLine(cue2);
        if (!a.length || !b.length) return false;
        if (!(isPrimarilyJapanese(a) && isPrimarilyJapanese(b))) return false;
        const joinedLen = [...normalizeJpCaptionLine(a + b)].length;
        if (joinedLen < MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE) return false;
        if (a.length < 14 || b.length < 12) return false;
        if (!firstCaptionVisualLineLooksSentenceComplete(a)) return false;
        return true;
      }
      function prefersContrastiveCueResplitOverNativeVL(cueHead) {
        const a = normalizeJpCaptionLine(cueHead);
        if (/(?:ですが|ってますが|ていますが|でしたが)[、，]/u.test(a)) return true;
        if (!/ですが[、，]/u.test(a)) return false;
        const tail = (/ですが[、，]\s*(.+)$/su.exec(a) ?? [])[1];
        const tailN = normalizeJpCaptionLine(tail ?? "");
        if (!tailN.length || tailN.length > 14) return false;
        return /^(今日は|明日は|今は|今回は|本日は|そのうえ|それでも|とはいえ|ちなみに)/u.test(
          tailN
        );
      }
      function prefersContrastiveTwoLineNativeResplit(cue1, cue2) {
        const a = normalizeJpCaptionLine(cue1);
        const b = normalizeJpCaptionLine(cue2);
        if (!a.length || !b.length) return false;
        const upperEndsContrastiveComma =
          /(?:ですが|ってますが|ていますが|でしたが|けれども?|だけど|だが|とはいえ|それでも)[、，]\s*$/u.test(a);
        if (!upperEndsContrastiveComma) return false;
        return /^(今日は|明日は|今は|今回は|本日は|そのうえ|それでも|とはいえ|ちなみに)/u.test(b);
      }
      function prefersRemergeShortLeadingTopicHaNativeVL(cue1, cue2) {
        const a = normalizeJpCaptionLine(cue1);
        const b = normalizeJpCaptionLine(cue2);
        if (!a.length || !b.length || a.length > 14) return false;
        const topicOnlyUpper = /^(昨日は|今は|今日は|明日は|先日は|本日は|今朝は|昨夜は)([、，])?$/u.test(
          a
        );
        if (!topicOnlyUpper) return false;
        return /(?:ですが|ってますが)[、，]/u.test(b) || /(けれども?|だけど|だが|とはいえ|それでも)[、，]/u.test(b);
      }
      /**
       * 「概念」レイヤーのレキシカル実装（語形・代表表記）。MeCab／kuromoji 級の形態素解析は未統合。
       * ブラウザ拡張では辞書サイズ・初期化コストが大きい（kuromoji 系で辞書十数 MB 級が一般的）。
       * 主改行は引き続き Budoux のモデル側、ここでは少数のキューだけを規則で上書きする。
       * 落下が「転落」「落ちた」等へ言い換えられたら JP_CUE_LEX を拡張する。
       */
      var JP_CUE_LEX = {
        torokoroComma: /\u3068\u3053\u308D[\u3001\uFF0C]/u,
        fallCompound: "\u843D\u4E0B",
        displaySurfaceDamageOrClosedUtterance: /(?:\u753B|\u986F|\u9867|\u9583)?\u9762|\u5272|\u7834|\u788E|\u58CA|\u307E\u3057\u305F|\u307E\u3059/u,
        thirdLineOutcome: /\u307E\u3057\u305F|\u307E\u3059|\u5272|\u7834|\u308F\u308C/u
      };
      function conceptTorokoroTemporalPauseWithComma(flatNorm) {
        return JP_CUE_LEX.torokoroComma.test(flatNorm);
      }
      function conceptObjectOrDeviceFall(flatNorm) {
        return flatNorm.includes(JP_CUE_LEX.fallCompound);
      }
      function conceptDisplayOrSurfaceDamageInFlat(flatNorm) {
        return JP_CUE_LEX.displaySurfaceDamageOrClosedUtterance.test(flatNorm);
      }
      function conceptThirdLineHasDamageOrPoliteClosure(lineNorm) {
        return JP_CUE_LEX.thirdLineOutcome.test(lineNorm);
      }
      function conceptMicrowaveDotsPreferReflow(flatNorm) {
        const f = normalizeJpCaptionLine(flatNorm);
        if (f.length < 40) return false;
        return (
          /(?:電子レンジ|電気ケトル)/u.test(f) &&
          /持ち込み[、，]/u.test(f) &&
          /ないのです/u.test(f) &&
          /であるならば/u.test(f) &&
          /このように出力を控えめに/u.test(f) &&
          /小型化/u.test(f) &&
          /助かります/u.test(f)
        );
      }
      function shouldForceReflowTorokoroFallScreenDamageCue(flatText) {
        const f = normalizeJpCaptionLine(flatText);
        if (!f.length) return false;
        return (
          conceptTorokoroTemporalPauseWithComma(f) &&
          conceptObjectOrDeviceFall(f) &&
          conceptDisplayOrSurfaceDamageInFlat(f)
        );
      }
      function shouldPreserveNativeMultilineCueLayout(lineTexts) {
        if (lineTexts.length < 2) return false;
        const flat = normalizeJpCaptionLine(lineTexts.join(""));
        if (flat.length < MIN_FLAT_CHARS_TO_REMERGE_NATIVE_MULTILINE) {
          if (lineTexts.length >= 3) return false;
          if (/(?:ですが|ってますが|ていますが|でしたが)[、，]/u.test(flat)) return false;
          const top = lineTexts[0];
          const second = lineTexts[1];
          if (top !== void 0 && prefersContrastiveCueResplitOverNativeVL(top)) return false;
          if (
            top !== void 0 &&
            second !== void 0 &&
            prefersContrastiveTwoLineNativeResplit(top, second)
          )
            return false;
          if (top !== void 0 && second !== void 0 && prefersRemergeShortLeadingTopicHaNativeVL(top, second))
            return false;
          if (shouldForceReflowTorokoroFallScreenDamageCue(flat)) return false;
          if (conceptMicrowaveDotsPreferReflow(flat)) return false;
          return true;
        }
        const c1 = lineTexts[0];
        const c2 = lineTexts[1];
        if (lineTexts.length === 2 && c1 !== void 0 && c2 !== void 0) {
          if (shouldPreserveNativeTwoLineSignoff(c1, c2)) return true;
          if (shouldPreserveNativeTwoLineParagraphBoundary(c1, c2)) return true;
        }
        return false;
      }
      function mergeTrailingPoliteDesuOnlyLine(lines) {
        if (lines.length < 3) return lines;
        const nLast = normalizeJpCaptionLine(lines[lines.length - 1] ?? "");
        if (!/^です[。．!！?？…」』\s　]*$/u.test(nLast)) return lines;
        const prev = lines[lines.length - 2] ?? "";
        const fused = normalizeJpCaptionLine(prev + nLast.replace(/^[\s　]+/u, ""));
        return [...lines.slice(0, -2), fused];
      }
      function firstCaptionVisualLineLooksSentenceComplete(line) {
        const t = normalizeJpCaptionLine(line);
        if (!t.length) return false;
        return /(です|ました|でした|ませんでした|いきません)[。．!！?？…」』\s　]*$/u.test(t) || /(だ|よ|わ)[。．!！?？…」』\s　]*$/u.test(t) || /[。．!！?？…」』][\s　]*$/u.test(t);
      }
      function shouldStackSentenceInitialVisualCue(originalLines, mergedPair) {
        if (shouldPreserveNativeMultilineCueLayout(originalLines)) return false;
        const [a, b] = mergedPair;
        if (!firstCaptionVisualLineLooksSentenceComplete(a)) return false;
        const bl = normalizeJpCaptionLine(b).length;
        if (bl < 20) return false;
        const lastOrig = normalizeJpCaptionLine(originalLines[originalLines.length - 1] ?? "");
        if (originalLines.length >= 3 && /^です[。．!！?？…」』\s　]*$/u.test(lastOrig))
          return true;
        return bl >= 34;
      }
      function tryStackedMultilineCaptionBudoux(lineTexts) {
        const parts = lineTexts.map(normalizeJpCaptionLine).filter((s) => s.length > 0);
        if (parts.length < 2) return null;
        const merged = mergeTrailingPoliteDesuOnlyLine(parts);
        if (merged.length !== 2) return null;
        const [head, tail] = merged;
        if (!head.length || !tail.length) return null;
        if (!shouldStackSentenceInitialVisualCue(lineTexts, [head, tail])) return null;
        if (!isPrimarilyJapanese(head) || !isPrimarilyJapanese(tail)) return null;
        const chunks = parseJpCaptionChunks(tail);
        if (!chunks.length) return null;
        const sub = balanceLines(chunks, maxCaptionLinesForFlatDisplay(tail));
        if (sub.length <= 1) return null;
        return [normalizeJpCaptionLine(head), ...sub.map((s) => normalizeJpCaptionLine(s))];
      }
      function splitCaptionHeadAtStrongPause(head) {
        const h = normalizeJpCaptionLine(head);
        const m = /^(.{10,}?(?:ところ|が|ので|けど|けれども|ですが|ますが)[、，])(.{10,})$/u.exec(h);
        if (!m) return null;
        const first = normalizeJpCaptionLine(m[1] ?? "");
        const second = normalizeJpCaptionLine(m[2] ?? "");
        if (!first || !second) return null;
        if (first.length > 34 || second.length > 34) return null;
        return [first, second];
      }
      function tryPoliteContinuationFlatBudoux(flatText) {
        const flat = normalizeJpCaptionLine(flatText);
        const m = /^([\s\S]+?(?:しません|できません|ありません|いきません|なりません|されません|れません|ませんでした))(とにかく[\s\S]+)$/u.exec(flat);
        if (!m) return null;
        const head = normalizeJpCaptionLine(m[1] ?? "");
        const tail = normalizeJpCaptionLine(m[2] ?? "");
        if (!head || !tail) return null;
        if (!isPrimarilyJapanese(head) || !isPrimarilyJapanese(tail)) return null;
        const headChunks = parseJpCaptionChunks(head);
        if (!headChunks.length) return null;
        const headLines = splitCaptionHeadAtStrongPause(head) ?? balanceLines(headChunks, maxCaptionLinesForTextLength([...head].length));
        if (headLines.length <= 1) return null;
        const tailChunks = parseJpCaptionChunks(tail);
        const tailLines = tailChunks.length ? balanceLines(tailChunks, maxCaptionLinesForTextLength([...tail].length)) : [tail];
        return [
          ...headLines.map((s) => captionStripCueBreakDotsAfterPolitenessCue(normalizeJpCaptionLine(s))),
          ...tailLines.map((s) => normalizeJpCaptionLine(s))
        ].filter(Boolean);
      }
      function tryPoliteContinuationCueBudoux(lineTexts) {
        const parts = lineTexts.map(normalizeJpCaptionLine).filter((s) => s.length > 0);
        if (parts.length < 3) return null;
        for (let i = 0; i < parts.length - 2; i++) {
          const left = parts[i] ?? "";
          const continuation = parts[i + 1] ?? "";
          if (!/(?:しま|できま|ありま|いきま|なりま|されま|れま|ませ)$/u.test(left)) continue;
          if (!/^せん(?:でした)?[。．!！?？…」』\s　]*$/u.test(continuation)) continue;
          const head = normalizeJpCaptionLine(parts.slice(0, i + 2).join(""));
          const tailLines = parts.slice(i + 2).map(normalizeJpCaptionLine).filter(Boolean);
          if (!tailLines.length) continue;
          if (!/(?:しません|できません|ありません|いきません|なりません|されません|れません|ませんでした)[。．!！?？…」』\s　]*$/u.test(head))
            continue;
          if (!isPrimarilyJapanese(head) || !tailLines.every(isPrimarilyJapanese)) continue;
          const chunks = parseJpCaptionChunks(head);
          if (!chunks.length) continue;
          const headLines = splitCaptionHeadAtStrongPause(head) ?? balanceLines(chunks, maxCaptionLinesForTextLength([...head].length));
          if (headLines.length <= 1) continue;
          return [
            ...headLines.map((s) => captionStripCueBreakDotsAfterPolitenessCue(normalizeJpCaptionLine(s))),
            ...tailLines
          ];
        }
        return null;
      }
      function tryGaContrastiveKokoMadeThreeLines(flatJoined) {
        const flat = normalizeJpCaptionLine(flatJoined);
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        const m = /^([\s\S]{10,}?(?:ですが|ってますが|ていますが|ますが|でしたが)[、，])([\s\S]{6,}?ここまで)((?:帰る|明日|本日|翌日|また)[\s\S]+)$/u.exec(flat);
        if (!m) return null;
        const one = normalizeJpCaptionLine(m[1] ?? "");
        const two = normalizeJpCaptionLine(m[2] ?? "");
        const three = normalizeJpCaptionLine(m[3] ?? "");
        if (!one || !two || !three) return null;
        if ([...one].length > 54 || [...two].length > 46 || [...three].length > 56 || [...three].length < 10)
          return null;
        return [one, two, three];
      }
      function tryContrastiveGaKaitaiNodeYattoFourLines(flatJoined) {
        const flat = normalizeJpCaptionLine(flatJoined);
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        const m = /^([\s\S]{10,}?(?:ですが|ってますが|ていますが|ますが|でしたが)[、，])([\s\S]{6,}?買いたい)(最近[\s\S]{8,}?ので[、，])(やっと[\s\S]+)$/u.exec(flat);
        if (!m) return null;
        const one = normalizeJpCaptionLine(m[1] ?? "");
        const two = normalizeJpCaptionLine(m[2] ?? "");
        const three = normalizeJpCaptionLine(m[3] ?? "");
        const four = normalizeJpCaptionLine(m[4] ?? "");
        if (!one || !two || !three || !four) return null;
        if (
          [...one].length > 48 ||
          [...two].length > 36 ||
          [...three].length > 52 ||
          [...four].length > 28 ||
          [...four].length < 6
        )
          return null;
        return [one, two, three, four];
      }
      function tryHaNiArimasuKoreHaDesugaYoriyoiFourLines(flatJoined) {
        const flat = normalizeJpCaptionLine(flatJoined);
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        if ([...flat].length < 48) return null;
        const m =
          /^([\s\S]{8,}?は)([\s\S]{10,}?にあります)\s*(これは[\s\S]{6,}?ですが[、，])([\s\S]{10,})$/u.exec(flat);
        if (!m) return null;
        const one = normalizeJpCaptionLine(m[1] ?? "");
        const two = normalizeJpCaptionLine(m[2] ?? "");
        const three = normalizeJpCaptionLine(m[3] ?? "");
        const four = normalizeJpCaptionLine(m[4] ?? "");
        if (!one || !two || !three || !four) return null;
        if (/は$/u.test(two)) return null;
        if (/これは/u.test(two)) return null;
        if (/^(?:それ|あれ)は/u.test(four)) return null;
        if ([...one].length > 42 || [...one].length < 9) return null;
        if ([...two].length > 36 || [...two].length < 10) return null;
        if ([...three].length > 52 || [...three].length < 8) return null;
        if ([...four].length > 56 || [...four].length < 8) return null;
        return [one, two, three, four];
      }
      function tryCompareToUsbAcWattHeToFourLines(flatJoined) {
        const flat = normalizeJpCaptionLine(flatJoined);
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        if ([...flat].length < 50) return null;
        const m =
          /^([\s\S]{4,}?と比較すると[、，])([\s\S]{12,}?に)(そして[\s\S]{10,}?へと)([\s\S]{10,})$/u.exec(flat);
        if (!m) return null;
        const one = normalizeJpCaptionLine(m[1] ?? "");
        const two = normalizeJpCaptionLine(m[2] ?? "");
        const three = normalizeJpCaptionLine(m[3] ?? "");
        const four = normalizeJpCaptionLine(m[4] ?? "");
        if (!one || !two || !three || !four) return null;
        if (/そして/u.test(two)) return null;
        if (!/^そして/u.test(three) || !/へと$/u.test(three)) return null;
        if (!/(?:出力|定格|USB|→|\d\s*W)/u.test(two)) return null;
        if (!/(?:至っては|出力|最大|\d)/u.test(three)) return null;
        if ([...one].length > 44 || [...one].length < 10) return null;
        if ([...two].length > 56 || [...two].length < 12) return null;
        if ([...three].length > 56 || [...three].length < 12) return null;
        if ([...four].length > 44 || [...four].length < 10) return null;
        return [one, two, three, four];
      }
      function canonMicrowaveDotsAfterNoDesu(flatNorm) {
        const s = normalizeJpCaptionLine(flatNorm);
        return s.replace(
          /(ないのです)[\s　]*(?:\.{3,}|\.{2}(?=\s*であるならば)|[\uFF0E\u3002]{2,}|[\u2026\u22EF⋯…]{2,}|[\u2026\u22EF⋯…][\s　]*[\u2026\u22EF⋯…])(?=\s*であるならば)/gu,
          "$1\u2026\u2026"
        );
      }
      function tryMochikomiNdeshiDotsOutputCompactFourLines(flatJoined) {
        const flat = canonMicrowaveDotsAfterNoDesu(flatJoined);
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        if ([...flat].length < 58) return null;
        if (!/(?:電子レンジ|電気ケトル)/u.test(flat)) return null;
        if (!/(?:持ち込み[、，]|持ち込み\s*[、，])/u.test(flat)) return null;
        if (!/ないのです\u2026\u2026であるならば/u.test(flat)) return null;
        if (!/(?:出力を控えめに|出力を.?控えめに)/u.test(flat)) return null;
        if (!/(?:小型化してもらった|小型化\s*してもらった)/u.test(flat)) return null;
        const m =
          /^([\s\S]+?持ち込み[、，])([\s\S]+?ないのです)(\u2026\u2026であるならば[、，]このように出力を控えめに)([\s\S]+)$/u.exec(flat);
        if (!m) return null;
        const one = normalizeJpCaptionLine(m[1] ?? "");
        const two = normalizeJpCaptionLine(m[2] ?? "");
        const three = normalizeJpCaptionLine(m[3] ?? "");
        const four = normalizeJpCaptionLine(m[4] ?? "");
        if (!one || !two || !three || !four) return null;
        if (!/^\u2026\u2026であるならば/u.test(three.replace(/^[\s　]+/u, ""))) return null;
        if (!/^小型化/ui.test(four) || !/助かります$/u.test(four)) return null;
        if ([...one].length > 52 || [...one].length < 12) return null;
        if ([...two].length > 56 || [...two].length < 12) return null;
        if ([...three].length > 58 || [...three].length < 14) return null;
        if ([...four].length > 52 || [...four].length < 10) return null;
        return [one, two, three, four];
      }
      function tryReflowTorokoroFallScreenDamageThreeLines(flatJoined) {
        const flat = normalizeJpCaptionLine(flatJoined).replace(/[\u2060\u200B\uFEFF]/gu, "");
        if (!flat.length || !isPrimarilyJapanese(flat)) return null;
        if (!conceptTorokoroTemporalPauseWithComma(flat) || !conceptObjectOrDeviceFall(flat)) return null;
        let oneText = "";
        let rest = "";
        const mComma = /^(.{10,}?ところ[、，])/u.exec(flat);
        if (mComma && mComma[1]) {
          oneText = normalizeJpCaptionLine(mComma[1]);
          rest = flat.slice(mComma[1].length);
        } else {
          const mBare = /^(.{10,}?ところ)(?=\u30E2)/u.exec(flat);
          if (!mBare || !mBare[1]) return null;
          oneText = normalizeJpCaptionLine(mBare[1] + "\u3001");
          rest = flat.slice(mBare[1].length);
        }
        if ([...oneText].length < 12) return null;
        const ir = rest.indexOf(JP_CUE_LEX.fallCompound);
        if (ir < 0) return null;
        const twoSlice = normalizeJpCaptionLine(rest.slice(0, ir + JP_CUE_LEX.fallCompound.length));
        const threeSlice = normalizeJpCaptionLine(rest.slice(ir + JP_CUE_LEX.fallCompound.length));
        if (!twoSlice || !threeSlice) return null;
        if ([...twoSlice].length < 6 || [...threeSlice].length < 8) return null;
        if (!conceptThirdLineHasDamageOrPoliteClosure(threeSlice)) return null;
        return [oneText, twoSlice, threeSlice];
      }
      function clearCaptionContentParentReflowLayout(parent) {
        if (!(parent instanceof HTMLElement)) return;
        [
          "display",
          "flex-direction",
          "justify-content",
          "align-items",
          "text-align",
          "width",
          "max-width",
          "margin-left",
          "margin-right",
          "box-sizing"
        ].forEach((p) => parent.style.removeProperty(p));
        const captionWindow = parent.closest(".ytp-caption-window, .caption-window");
        if (captionWindow instanceof HTMLElement) clearCaptionWindowReflowLayout(captionWindow);
      }
      function clearCaptionWindowReflowLayout(captionWindow) {
        if (!(captionWindow instanceof HTMLElement)) return;
        [
          "display",
          "flex-direction",
          "align-items",
          "justify-content",
          "text-align",
          "width",
          "max-width",
          "min-width",
          "margin-left",
          "margin-right",
          "padding-left",
          "padding-right",
          "box-sizing",
          "left",
          "right",
          "transform"
        ].forEach((p) => captionWindow.style.removeProperty(p));
      }
      function restoreStyleAttribute(el, styleText) {
        if (!(el instanceof HTMLElement)) return;
        if (styleText === null) {
          el.removeAttribute("style");
          return;
        }
        el.setAttribute("style", styleText);
      }
      function makeCaptionBackup(parent, captionWindow, textHash) {
        return {
          html: parent.innerHTML,
          textHash,
          parentStyle: parent instanceof HTMLElement ? parent.getAttribute("style") : null,
          windowStyle: captionWindow instanceof HTMLElement ? captionWindow.getAttribute("style") : null
        };
      }
      function ensureCaptionBackupForHash(parent, captionWindow, textHash) {
        if (
          !captionWindow.hasAttribute(ATTR_DONE) ||
          !captionBackup ||
          captionBackup.textHash !== textHash
        ) {
          captionBackup = makeCaptionBackup(parent, captionWindow, textHash);
        }
      }
      function applyCaptionContentParentReflowLayout(parent) {
        if (!(parent instanceof HTMLElement)) return;
        parent.style.setProperty("display", "flex", "important");
        parent.style.setProperty("flex-direction", "column", "important");
        parent.style.setProperty("align-items", "center", "important");
        parent.style.setProperty("justify-content", "center", "important");
        parent.style.setProperty("text-align", "center", "important");
        parent.style.setProperty("width", "100%", "important");
        parent.style.setProperty("max-width", "100%", "important");
        parent.style.setProperty("margin-left", "auto", "important");
        parent.style.setProperty("margin-right", "auto", "important");
        parent.style.setProperty("box-sizing", "border-box", "important");
        const captionWindow = parent.closest(".ytp-caption-window, .caption-window");
        if (captionWindow instanceof HTMLElement) applyCaptionWindowReflowLayout(captionWindow);
      }
      function applyCaptionWindowReflowLayout(captionWindow) {
        if (!(captionWindow instanceof HTMLElement)) return;
        captionWindow.style.setProperty("left", "50%", "important");
        captionWindow.style.setProperty("right", "auto", "important");
        captionWindow.style.setProperty("width", "fit-content", "important");
        captionWindow.style.setProperty("max-width", "calc(100% - 48px)", "important");
        captionWindow.style.setProperty("min-width", "0", "important");
        captionWindow.style.setProperty("margin-left", "0", "important");
        captionWindow.style.setProperty("margin-right", "0", "important");
        captionWindow.style.setProperty("padding-left", "0", "important");
        captionWindow.style.setProperty("padding-right", "0", "important");
        captionWindow.style.setProperty("box-sizing", "border-box", "important");
        captionWindow.style.setProperty("text-align", "center", "important");
        captionWindow.style.setProperty("display", "flex", "important");
        captionWindow.style.setProperty("flex-direction", "column", "important");
        captionWindow.style.setProperty("align-items", "center", "important");
        captionWindow.style.setProperty("justify-content", "center", "important");
        captionWindow.style.setProperty("transform", "translateX(-50%)", "important");
      }
      function centerCaptionWithoutReflow(parent, captionWindow, rawText) {
        const raw = normalizeJpCaptionLine(rawText);
        if (!raw || !isPrimarilyJapanese(raw)) return false;
        const h = hashText(raw);
        ensureCaptionBackupForHash(parent, captionWindow, h);
        applyCaptionContentParentReflowLayout(parent);
        captionWindow.setAttribute(ATTR_HASH, h);
        captionWindow.setAttribute(ATTR_DONE, "1");
        return true;
      }
      function captionLineWithSegment(lineText, inheritedStyle) {
        const line = document.createElement("div");
        line.className = "caption-visual-line";
        line.style.setProperty("display", "flex", "important");
        line.style.setProperty("justify-content", "center", "important");
        line.style.setProperty("align-items", "center", "important");
        line.style.setProperty("width", "100%", "important");
        line.style.setProperty("max-width", "100%", "important");
        line.style.setProperty("box-sizing", "border-box", "important");
        line.style.setProperty("text-align", "center", "important");
        line.style.setProperty("margin-left", "auto", "important");
        line.style.setProperty("margin-right", "auto", "important");
        const seg = document.createElement("span");
        seg.className = "ytp-caption-segment";
        seg.textContent = glueInseparableJpCompounds(
          readableJpCaptionInsertHalfwidthSpaces(captionStripCueBreakDotsAfterPolitenessCue(lineText))
        );
        if (inheritedStyle) seg.setAttribute("style", inheritedStyle);
        seg.style.setProperty("position", "static", "important");
        seg.style.setProperty("display", "inline-block", "important");
        seg.style.setProperty("text-align", "center", "important");
        seg.style.setProperty("max-width", "100%", "important");
        seg.style.setProperty("white-space", "normal", "important");
        seg.style.setProperty("margin-left", "auto", "important");
        seg.style.setProperty("margin-right", "auto", "important");
        seg.style.removeProperty("left");
        seg.style.removeProperty("right");
        seg.style.removeProperty("transform");
        line.appendChild(seg);
        return line;
      }
      function findCaptionWindow() {
        for (const sel of CAPTION_WINDOW_SELECTORS) {
          const el = document.querySelector(sel);
          if (el instanceof HTMLElement) return el;
        }
        const fromPlayer = [
          ".ytp-caption-window-container .ytp-caption-window",
          ".ytp-caption-window-container .caption-window"
        ];
        for (const sel of fromPlayer) {
          const el = queryInMoviePlayer(sel);
          if (el instanceof HTMLElement) return el;
        }
        const seen = /* @__PURE__ */ new Set();
        const queue = [];
        for (const root of [
          document.querySelector("#movie_player"),
          document.querySelector("ytd-player"),
          document.querySelector("ytd-shorts"),
          document.querySelector("ytd-watch-flexy"),
          document.body,
          document.documentElement
        ]) {
          if (root) {
            queue.push(root);
            if (root.shadowRoot) queue.push(root.shadowRoot);
          }
        }
        while (queue.length) {
          const n = queue.shift();
          if (!n || seen.has(n)) continue;
          seen.add(n);
          for (const sel of fromPlayer) {
            const hit = n.querySelector?.(sel);
            if (hit instanceof HTMLElement) return hit;
          }
          n.querySelectorAll?.("*").forEach((el) => {
            if (el instanceof Element && el.shadowRoot && !seen.has(el.shadowRoot))
              queue.push(el.shadowRoot);
          });
        }
        return null;
      }
      function findContentParent(captionWindow) {
        const inner = captionWindow.querySelector(".ytp-caption-window-inner");
        if (inner instanceof HTMLElement) return inner;
        const captionsText = captionWindow.querySelector(".captions-text");
        if (captionsText instanceof HTMLElement) return captionsText;
        return captionWindow;
      }
      function restoreOriginalIfNeeded(captionWindow) {
        if (!captionWindow.hasAttribute(ATTR_DONE) || !captionBackup) return;
        const parent = findContentParent(captionWindow);
        parent.innerHTML = captionBackup.html;
        restoreStyleAttribute(parent, captionBackup.parentStyle);
        restoreStyleAttribute(captionWindow, captionBackup.windowStyle);
        captionWindow.removeAttribute(ATTR_DONE);
        captionWindow.removeAttribute(ATTR_HASH);
        captionBackup = null;
      }
      function applyBudouxToWindow(captionWindow) {
        const parentProbe = findContentParent(captionWindow);
        const lineTextsPre = extractCaptionVisualLineTexts(parentProbe);
        const flatJoinForRules =
          lineTextsPre.length > 0 ? lineTextsPre.join("") : collectCaptionText(parentProbe);
        if (
          captionWindow.hasAttribute(ATTR_DONE) &&
          captionWindow.getAttribute(ATTR_HASH) === hashText(normalizeJpCaptionLine(flatJoinForRules))
        ) {
          return;
        }
        if (shouldPreserveNativeMultilineCueLayout(lineTextsPre)) {
          if (!centerCaptionWithoutReflow(parentProbe, captionWindow, flatJoinForRules)) {
            captionWindow.removeAttribute(ATTR_HASH);
            captionWindow.removeAttribute(ATTR_DONE);
            captionBackup = null;
            clearCaptionContentParentReflowLayout(parentProbe);
          }
          return;
        }
        const flatForTokoro = flatJoinForRules;
        const flatCollectedTok = normalizeJpCaptionLine(collectCaptionText(parentProbe));
        let tokoroRakkaThree = tryReflowTorokoroFallScreenDamageThreeLines(flatForTokoro);
        let tokoroCanonFlat = flatForTokoro;
        if (!tokoroRakkaThree) {
          const tr2 = tryReflowTorokoroFallScreenDamageThreeLines(flatCollectedTok);
          if (tr2) {
            tokoroRakkaThree = tr2;
            tokoroCanonFlat = flatCollectedTok;
          }
        }
        if (tokoroRakkaThree && tokoroRakkaThree.length >= 3) {
          const rawCanonTr = normalizeJpCaptionLine(tokoroCanonFlat);
          if (!rawCanonTr || !isPrimarilyJapanese(rawCanonTr)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hTr = hashText(rawCanonTr);
          const parentTr = parentProbe;
          const sampleSegTr = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyleTr = sampleSegTr instanceof HTMLElement ? sampleSegTr.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentTr, captionWindow, hTr);
          }
          parentTr.replaceChildren();
          for (const lineText of tokoroRakkaThree) {
            parentTr.appendChild(captionLineWithSegment(lineText, inheritedStyleTr));
          }
          applyCaptionContentParentReflowLayout(parentTr);
          captionWindow.setAttribute(ATTR_HASH, hTr);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const politeContinuationCaptionLines = tryPoliteContinuationCueBudoux(lineTextsPre) ?? tryPoliteContinuationFlatBudoux(flatJoinForRules);
        if (politeContinuationCaptionLines && politeContinuationCaptionLines.length >= 2) {
          const rawCanon = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanon || !isPrimarilyJapanese(rawCanon)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hPolite = hashText(rawCanon);
          const parentPolite = parentProbe;
          const sampleSegPolite = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStylePolite = sampleSegPolite instanceof HTMLElement ? sampleSegPolite.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentPolite, captionWindow, hPolite);
          }
          parentPolite.replaceChildren();
          for (const lineText of politeContinuationCaptionLines) {
            parentPolite.appendChild(captionLineWithSegment(lineText, inheritedStylePolite));
          }
          applyCaptionContentParentReflowLayout(parentPolite);
          captionWindow.setAttribute(ATTR_HASH, hPolite);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const kokomadeGaThree = tryGaContrastiveKokoMadeThreeLines(flatJoinForRules);
        if (kokomadeGaThree && kokomadeGaThree.length >= 3) {
          const rawCanonKo = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanonKo || !isPrimarilyJapanese(rawCanonKo)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hKo = hashText(rawCanonKo);
          const parentKo = parentProbe;
          const sampleSegKo = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyleKo = sampleSegKo instanceof HTMLElement ? sampleSegKo.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentKo, captionWindow, hKo);
          }
          parentKo.replaceChildren();
          for (const lineText of kokomadeGaThree) {
            parentKo.appendChild(captionLineWithSegment(lineText, inheritedStyleKo));
          }
          applyCaptionContentParentReflowLayout(parentKo);
          captionWindow.setAttribute(ATTR_HASH, hKo);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const gaKaitaiFour = tryContrastiveGaKaitaiNodeYattoFourLines(flatJoinForRules);
        if (gaKaitaiFour && gaKaitaiFour.length >= 4) {
          const rawCanon4 = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanon4 || !isPrimarilyJapanese(rawCanon4)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const h4 = hashText(rawCanon4);
          const parent4 = parentProbe;
          const sampleSeg4 = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyle4 = sampleSeg4 instanceof HTMLElement ? sampleSeg4.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parent4, captionWindow, h4);
          }
          parent4.replaceChildren();
          for (const lineText of gaKaitaiFour) {
            parent4.appendChild(captionLineWithSegment(lineText, inheritedStyle4));
          }
          applyCaptionContentParentReflowLayout(parent4);
          captionWindow.setAttribute(ATTR_HASH, h4);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const compareUsbAcWattFour = tryCompareToUsbAcWattHeToFourLines(flatJoinForRules);
        if (compareUsbAcWattFour && compareUsbAcWattFour.length >= 4) {
          const rawCanonCu = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanonCu || !isPrimarilyJapanese(rawCanonCu)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hCu = hashText(rawCanonCu);
          const parentCu = parentProbe;
          const sampleSegCu = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyleCu = sampleSegCu instanceof HTMLElement ? sampleSegCu.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentCu, captionWindow, hCu);
          }
          parentCu.replaceChildren();
          for (const lineText of compareUsbAcWattFour) {
            parentCu.appendChild(captionLineWithSegment(lineText, inheritedStyleCu));
          }
          applyCaptionContentParentReflowLayout(parentCu);
          captionWindow.setAttribute(ATTR_HASH, hCu);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        let microwaveEllipsisFour = tryMochikomiNdeshiDotsOutputCompactFourLines(flatJoinForRules);
        let microwaveCanonFlat = flatJoinForRules;
        if (!microwaveEllipsisFour || microwaveEllipsisFour.length < 4) {
          const collectedMw = normalizeJpCaptionLine(collectCaptionText(parentProbe));
          microwaveEllipsisFour = tryMochikomiNdeshiDotsOutputCompactFourLines(collectedMw);
          if (microwaveEllipsisFour && microwaveEllipsisFour.length >= 4)
            microwaveCanonFlat = collectedMw;
        }
        if (microwaveEllipsisFour && microwaveEllipsisFour.length >= 4) {
          const rawCanonMe = normalizeJpCaptionLine(microwaveCanonFlat);
          if (!rawCanonMe || !isPrimarilyJapanese(rawCanonMe)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hMe = hashText(rawCanonMe);
          const parentMe = parentProbe;
          const sampleSegMe = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyleMe = sampleSegMe instanceof HTMLElement ? sampleSegMe.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentMe, captionWindow, hMe);
          }
          parentMe.replaceChildren();
          for (const lineText of microwaveEllipsisFour) {
            parentMe.appendChild(captionLineWithSegment(lineText, inheritedStyleMe));
          }
          applyCaptionContentParentReflowLayout(parentMe);
          captionWindow.setAttribute(ATTR_HASH, hMe);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const haMasuPreferenceFour = tryHaNiArimasuKoreHaDesugaYoriyoiFourLines(flatJoinForRules);
        if (haMasuPreferenceFour && haMasuPreferenceFour.length >= 4) {
          const rawCanonHm = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanonHm || !isPrimarilyJapanese(rawCanonHm)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const hHm = hashText(rawCanonHm);
          const parentHm = parentProbe;
          const sampleSegHm = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyleHm = sampleSegHm instanceof HTMLElement ? sampleSegHm.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parentHm, captionWindow, hHm);
          }
          parentHm.replaceChildren();
          for (const lineText of haMasuPreferenceFour) {
            parentHm.appendChild(captionLineWithSegment(lineText, inheritedStyleHm));
          }
          applyCaptionContentParentReflowLayout(parentHm);
          captionWindow.setAttribute(ATTR_HASH, hHm);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        const stackedCaptionLines = tryStackedMultilineCaptionBudoux(lineTextsPre);
        if (stackedCaptionLines && stackedCaptionLines.length >= 2) {
          const rawCanon = normalizeJpCaptionLine(flatJoinForRules);
          if (!rawCanon || !isPrimarilyJapanese(rawCanon)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
            return;
          }
          const h2 = hashText(rawCanon);
          const parent2 = parentProbe;
          const sampleSeg2 = captionWindow.querySelector(".ytp-caption-segment");
          const inheritedStyle2 = sampleSeg2 instanceof HTMLElement ? sampleSeg2.getAttribute("style") : null;
          if (!captionWindow.hasAttribute(ATTR_DONE)) {
            captionBackup = makeCaptionBackup(parent2, captionWindow, h2);
          }
          parent2.replaceChildren();
          for (const lineText of stackedCaptionLines) {
            parent2.appendChild(captionLineWithSegment(lineText, inheritedStyle2));
          }
          applyCaptionContentParentReflowLayout(parent2);
          captionWindow.setAttribute(ATTR_HASH, h2);
          captionWindow.setAttribute(ATTR_DONE, "1");
          return;
        }
        if (lineTextsPre.length === 2 && !captionWindow.hasAttribute(ATTR_DONE)) {
          const [cue1, cue2] = lineTextsPre;
          if (
            looksLikeLongCuePlusShortTailLine(cue1, cue2) &&
            cue1 !== void 0 &&
            !prefersContrastiveCueResplitOverNativeVL(cue1)
          ) {
            const raw1 = normalizeJpCaptionLine(cue1);
            if (raw1.length <= 22) {
              captionWindow.removeAttribute(ATTR_HASH);
              clearCaptionContentParentReflowLayout(parentProbe);
              return;
            }
            const parent2 = parentProbe;
            const sampleSeg2 = captionWindow.querySelector(".ytp-caption-segment");
            const inheritedStyle2 = sampleSeg2 instanceof HTMLElement ? sampleSeg2.getAttribute("style") : null;
            const chunks2 = parseJpCaptionChunks(raw1);
            const linesPart = chunks2.length ? balanceLines(chunks2, maxCaptionLinesForFlatDisplay(raw1)) : [raw1];
            if (linesPart.length <= 1) {
              captionWindow.removeAttribute(ATTR_DONE);
              captionWindow.removeAttribute(ATTR_HASH);
              clearCaptionContentParentReflowLayout(parentProbe);
              return;
            }
            const visuals = parent2.querySelectorAll(".caption-visual-line");
            if (!(visuals.length >= 2)) {
              captionWindow.removeAttribute(ATTR_HASH);
              clearCaptionContentParentReflowLayout(parentProbe);
              return;
            }
            const flatBoth = normalizeJpCaptionLine(cue1 + cue2);
            const hBoth = hashText(flatBoth);
            if (!captionWindow.hasAttribute(ATTR_DONE)) {
              captionBackup = makeCaptionBackup(parent2, captionWindow, hBoth);
            }
            const insertBeforeSecond = visuals[1];
            const firstRemoved = visuals[0];
            parent2.removeChild(firstRemoved);
            for (let i = 0; i < linesPart.length; i++) {
              const lineEl = captionLineWithSegment(
                linesPart[i] ?? "",
                inheritedStyle2
              );
              parent2.insertBefore(lineEl, insertBeforeSecond);
            }
            applyCaptionContentParentReflowLayout(parent2);
            captionWindow.setAttribute(ATTR_HASH, hBoth);
            captionWindow.setAttribute(ATTR_DONE, "1");
            return;
          }
        }
        const raw = normalizeJpCaptionLine(flatJoinForRules);
        if (!raw || !isPrimarilyJapanese(raw)) {
          captionWindow.removeAttribute(ATTR_DONE);
          captionWindow.removeAttribute(ATTR_HASH);
          clearCaptionContentParentReflowLayout(parentProbe);
          return;
        }
        const h = hashText(raw);
        const chunks = parseJpCaptionChunks(raw);
        if (!chunks.length) {
          centerCaptionWithoutReflow(parentProbe, captionWindow, raw);
          return;
        }
        const lines = balanceLines(chunks, maxCaptionLinesForFlatDisplay(raw));
        if (lines.length <= 1) {
          if (!centerCaptionWithoutReflow(parentProbe, captionWindow, raw)) {
            captionWindow.removeAttribute(ATTR_DONE);
            captionWindow.removeAttribute(ATTR_HASH);
            clearCaptionContentParentReflowLayout(parentProbe);
          }
          return;
        }
        const parent = findContentParent(captionWindow);
        const sampleSeg = captionWindow.querySelector(".ytp-caption-segment");
        const inheritedStyle = sampleSeg instanceof HTMLElement ? sampleSeg.getAttribute("style") : null;
        if (!captionWindow.hasAttribute(ATTR_DONE)) {
          captionBackup = makeCaptionBackup(parent, captionWindow, h);
        }
        parent.replaceChildren();
        for (const lineText of lines) {
          parent.appendChild(captionLineWithSegment(lineText, inheritedStyle));
        }
        applyCaptionContentParentReflowLayout(parent);
        captionWindow.setAttribute(ATTR_HASH, h);
        captionWindow.setAttribute(ATTR_DONE, "1");
      }
      function processCaptionsNow() {
        ensureVideoCaptionListeners();
        ensureToggleInjected();
        const win = findCaptionWindow();
        if (!win) return;
        try {
          if (!budouxCaptionEnabled) {
            restoreOriginalIfNeeded(win);
            return;
          }
          applyBudouxToWindow(win);
        } catch {
        }
      }
      var debounce = null;
      function scheduleProcess() {
        if (debounce !== null) clearTimeout(debounce);
        debounce = setTimeout(() => {
          debounce = null;
          processCaptionsNow();
        }, 80);
      }
      function attachObserver(root) {
        const observer = new MutationObserver(scheduleProcess);
        observer.observe(root, {
          childList: true,
          subtree: true,
          characterData: true
        });
      }
      var attachAttempts = 0;
      function tryAttachToPlayer() {
        const player = document.querySelector("#movie_player");
        if (player instanceof HTMLElement) {
          attachObserver(player);
          scheduleProcess();
          return;
        }
        attachAttempts += 1;
        if (attachAttempts > 200) {
          attachObserver(document.documentElement);
          scheduleProcess();
          return;
        }
        requestAnimationFrame(tryAttachToPlayer);
      }
      function isLikelyVideoSurface() {
        const u = `${location.pathname}${location.search}`;
        return /\/watch(\?|\/)/.test(u) || /\/shorts\//.test(location.pathname) || /\/live(\/|$)/.test(location.pathname) || /\/embed\//.test(location.pathname);
      }
      if (isLikelyVideoSurface()) {
        ensureBudouxToggleStyles();
        tryAttachToPlayer();
        document.addEventListener("yt-navigate-finish", () => staggeredScheduleProcess());
      }
    }
  });
  require_content();
})();
/*! Bundled license information:

budoux/module/dom-browser.js:
budoux/module/parser.js:
budoux/module/html_processor.js:
budoux/module/index.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *     https://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
*/
